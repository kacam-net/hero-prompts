# 后端系统设计：套路、模式与坑

> 用途：整理「秒杀 / 支付 / 短链……」这类面试常考系统设计题背后的**可复用套路**、
> **每类题的必答点与深水区**，以及**做真实项目时会踩的坑**。
> 后续计划作为素材沉淀到问题池（见最后一节）。

---

## 零、先回答那个问题：这些题真的有套路吗？

**有，但「套路」这个词容易被理解错，理解错了就会在面试里翻车。**

准确的说法是三层：

1. **约束是固定的。** 秒杀永远是「读多写少 + 瞬时洪峰 + 超卖不可接受」，支付永远是
   「钱不能错 + 跨系统 + 网络会超时 + 对方会重发」。约束固定，所以能用的手段就那么几种，
   看起来像套路。
2. **套路是「约束 → 解法」的推导链，不是解法清单。** 「秒杀要用 Redis 预扣库存」是结论，
   「因为库存是单点热点、DB 行锁扛不住 5 万 QPS，所以把扣减挪到内存，代价是要解决
   Redis 与 DB 的一致性和宕机丢数据」才是推导。面试官考的是推导，不是结论。
3. **真正区分候选人的是第二层追问。** 第一层（「怎么防超卖」）人人都会背；
   第二层（「Redis 扣成功了但 MQ 消息丢了怎么办」「用户重复点了两次怎么办」
   「预扣了但 30 分钟没付款怎么办」）就开始筛人了。

所以对「有固定问题需要解决」这个判断：**完全正确**。每类系统确实有一份「不解决就是设计错误」
的必答清单。但对「有固定套路要遵守」这半句要小心 —— 背下套路而讲不出为什么，
面试官一层追问就会发现你在背题。**判断标准：你能不能说出每个决策的代价。**
只讲收益不讲代价的答案，一定是背的。

### 面试官实际在考的四件事

| 考点 | 表现 | 背题的人会怎样 |
| --- | --- | --- |
| **能不能识别核心矛盾** | 上来先问规模和一致性要求，然后说「这题的难点在 X」 | 直接开始画架构图 |
| **能不能做取舍并说出代价** | 「用 A，代价是 B，因为我们更怕 C」 | 「用 Redis / 用 MQ / 用分库分表」，没有代价 |
| **想不想得到失败路径** | 主动讲超时、重复、部分成功、宕机、重放 | 只讲 happy path，被追问才补 |
| **有没有真的做过** | 说得出具体数字、具体故障、具体的妥协 | 全是「一般来说」「通常我们会」 |

最后一条最难伪装，也最值钱。所以这份文档除了套路，还专门有一节写**真实项目的坑** ——
那些东西背不出来，只能踩过或者读过别人的复盘。

---

## 一、通用骨架：任何系统设计题都能套的六步

45 分钟的题，时间大致这么分。**最常见的失分不是答错，是节奏错**：花 25 分钟纠结数据库选型，
剩 10 分钟被追问失败路径，全线崩。

| 步骤 | 时间 | 干什么 |
| --- | --- | --- |
| 1. 澄清边界 | 5 min | 问清楚做什么、不做什么、多大规模、错了会怎样 |
| 2. 容量估算 | 3 min | 算出 QPS、存储、带宽，用它决定架构复杂度 |
| 3. 接口与数据模型 | 7 min | 先定 API 和表结构，架构图是它的推论 |
| 4. 高层架构 | 8 min | 一条主链路端到端走通 |
| 5. 深挖 | 15 min | 面试官会挑一个点往下钻，这是主战场 |
| 6. 失败与演进 | 7 min | 容灾、监控、扩容、灰度、回滚 |

### 1. 澄清边界：这一步不做，后面全是猜

必问的六个问题（不用全问，挑与本题相关的）：

- **功能边界**：核心链路是哪几步？哪些明确不做？（例：秒杀做不做退款？做不做预约？）
- **规模**：DAU 多少？峰值 QPS 多少？数据量多大？增长多快？
- **读写比**：读多写少（Feed、商品页）还是写多（IM、日志）？决定缓存策略和分片方式。
- **一致性要求**：能接受最终一致吗？延迟多久可接受？**哪一个字段绝对不能错？**
  （支付的金额不能错，但「已支付订单数」这个统计值晚 5 秒无所谓。
  一致性要求是**按字段**而不是按系统提的，这句话本身就是加分项。）
- **可用性要求**：几个 9？挂了是「不可用」还是「出错」？宁可拒绝服务还是宁可数据不一致？
- **谁在用**：C 端（要抗峰值、要低延迟）还是 B 端 / 内部（要正确、要可查、可对账）？

> **加分表达**：「我先明确一下这个系统的失败模式 —— 是宁可少卖不能超卖，
> 还是宁可超卖也不能让用户点不动？这决定了我把强一致放在哪一层。」

### 2. 容量估算：用来决定「要不要上复杂方案」

估算不是为了算准，是为了**证明某个方案是必要的或者是过度设计**。

几个背下来就够用的数字：

- 一天 ≈ 86400 秒，粗算按 **10 万秒**（好算，且天然留了余量）。
- **峰值 ≈ 平均值 × 2~10**（普通业务 2~3 倍，有营销活动 10 倍以上，秒杀是 1000 倍）。
- 单台 MySQL：简单点查 **几千~1万 QPS**，写入 **1000~5000 TPS**（受磁盘和事务影响大）。
- 单台 Redis：**约 10 万 QPS**（单线程，pipeline / 多实例可更高）。
- 单核处理简单 HTTP 请求：**几百~几千 QPS**，一台 8 核应用机 **几千 QPS** 量级。
- 一条订单记录 ≈ **0.5~1 KB**；一亿条 ≈ **50~100 GB**（不含索引，索引再乘 1.5~2）。
- **单表超过 1000 万~5000 万行**开始要认真考虑分表（不是硬阈值，取决于索引和查询模式）。
- 跨机房 RTT **几十 ms**，同机房 **0.5~2 ms**，内存访问 **纳秒级**，SSD 随机读 **~100 μs**。

估算的三句话模板：

> 「DAU 1000 万，人均 10 次请求 = 1 亿次 / 天 ≈ 1000 QPS 平均，峰值按 5 倍算 5000 QPS。
> 这个量单机 Redis 扛得住，MySQL 需要读写分离 + 几个从库，**还不到分库分表的程度**。
> 存储上一年 3.6 亿条 × 1KB ≈ 360 GB，**这个量需要考虑冷热分离或者按时间分表**。」

**主动说出「这个量还不需要 X」比堆技术更能证明你懂。**

### 3. 接口与数据模型：架构图是它的推论

很多人上来画框框，其实**先定 API 和表，架构自然就出来了**。

- **API**：路径、方法、关键参数、返回、幂等键。C 端接口一定要问「重复调用会怎样」。
- **表**：主键怎么生成、有哪些唯一约束、按什么分片、哪些字段要索引、状态字段有哪些取值。
- **唯一约束是设计的一部分，不是实现细节**。「订单号 + 商户号 唯一」这一条能挡掉一大半重复问题。

### 4. 高层架构：先把一条主链路端到端走通

客户端 → 接入层（LB / 网关：鉴权、限流）→ 应用层（无状态）→ 缓存 → 存储，
异步的部分挂 MQ，离线的部分走定时任务 / 数仓。

**先画一条链路走通，再加分支。** 一开始就画满十几个框，讲不完也讲不清。

### 5. 深挖：主战场

面试官会挑一个点让你往下钻。你可以主动引导他钻你准备最充分的那个：
「这里我认为最难的是 X，我先展开讲 X？」

### 6. 失败与演进：区分「写过业务」和「维护过系统」

- **依赖挂了怎么办**：DB 挂、缓存挂、MQ 挂、下游第三方挂 —— 分别是降级、限流还是拒绝？
- **怎么发现**：监控什么指标？报警阈值？（说得出「我会盯 P99 而不是平均值」就是懂的人）
- **怎么恢复**：能不能重放？有没有对账？数据错了怎么修（补偿脚本 / 人工工单）？
- **怎么演进**：量涨 10 倍先动哪一块？分片键选错了怎么迁移？

---

## 二、横切套路库：真正被反复复用的 12 件事

这一节是「套路」的核心。**所有高频题都是这 12 件事的不同组合。**
掌握这一节，遇到没见过的题也能推。

### 1. 幂等：分布式系统的第一课

**为什么必须有**：网络超时的本质是「不知道对方成功没有」，所以重试是必然的，
重复请求也是必然的。**任何写接口都要能被安全地重复调用。**

常见做法（从弱到强）：

| 手段 | 怎么做 | 适用 |
| --- | --- | --- |
| 唯一索引 | 业务唯一键（订单号、请求号）建唯一索引，重复插入报错即视为成功 | **最可靠，首选** |
| 状态机 | `UPDATE ... WHERE state = '待支付'`，看影响行数 | 状态流转类 |
| Token / 幂等键 | 客户端先领 token，提交时带上，服务端消费一次 | 表单重复提交 |
| 分布式锁 | 加锁串行化 | 兜底，不是主力 |
| 去重表 | 记录 request_id，插入成功才执行 | 消息消费去重 |

**坑与追问**：

- **「幂等」不等于「拒绝重复」，而是「重复调用返回同样的结果」。** 第二次调用直接报
  「重复请求」并返回失败，会让重试方误以为真的失败了 —— 正确做法是**返回第一次的结果**。
- **幂等键由谁生成？** 必须是**调用方**生成并在重试时保持不变。服务端生成的一定不行。
- **幂等键的有效期？** 存 Redis 有过期，过期后重放会穿透 —— 涉及钱的一定要落库。
- **先插去重表还是先执行业务？** 先插但业务失败会导致「记了但没做」，
  必须在同一个事务里，或者用「插入 → 执行 → 标记完成」三态。
- **并发的两个相同请求**同时进来，去重表的检查-插入不是原子的 —— 靠唯一索引，不靠先查后插。

### 2. 一致性：钱和库存永远绕不开

选择顺序（**先想能不能不分布式**）：

1. **单库事务**：能用一个数据库事务解决就别搞分布式事务。把强相关的数据放在同一个库、
   同一个分片，是最有效的「架构级简化」。
2. **最终一致 + 对账**：绝大多数真实系统的选择。允许中间态，靠异步补偿收敛，靠对账兜底。
3. **本地消息表 / 事务消息**：业务写库和「发消息」放进同一个本地事务，
   再由后台任务投递。解决「库写了消息没发」和「消息发了库没写」。
4. **TCC**（Try-Confirm-Cancel）：业务侵入大，适合内部核心链路（资金、库存）。
5. **Saga**：长流程，每步有补偿动作，适合跨多个服务的业务流程。
6. **2PC / XA**：面试可以提，实际很少用（阻塞、协调者单点、性能差）。

**坑与追问**：

- **「最终一致」要说出「最终是多久」**。10 秒和 1 小时是完全不同的产品体验。
- **补偿动作也会失败**。补偿必须幂等，且必须有重试上限 + 死信 + 人工兜底。
- **业务上有些操作不可补偿**（短信已发出、钱已到对方账户）—— 这类操作必须放在流程最后，
  或者设计成「可冲正」而不是「可撤销」。
- **对账是最后一道防线，不是可选项**。凡是涉及钱和库存的系统，必须有 T+1 对账 +
  差异处理流程。说不出对账的支付方案，是不完整的。
- **TCC 的三个经典问题**：空回滚（Cancel 先于 Try 到达）、悬挂（Try 在 Cancel 之后到达）、
  幂等。能说出这三个词就说明真做过。

### 3. 缓存：最容易讲得浅的一块

**三个经典问题**（人人会背，所以要讲得比别人深一层）：

| 问题 | 是什么 | 解法 | 更深一层 |
| --- | --- | --- | --- |
| **穿透** | 查一个根本不存在的 key，每次都打到 DB | 缓存空值（短 TTL）、布隆过滤器 | 空值也会被恶意打满内存；布隆过滤器不能删元素，要用计数布隆或定期重建 |
| **击穿** | 热点 key 过期瞬间大量请求打到 DB | 互斥锁重建、逻辑过期不物理过期 | 互斥锁会让其他请求排队 —— 要么返回旧值，要么设短超时快速失败 |
| **雪崩** | 大量 key 同时过期 / 缓存整体挂了 | TTL 加随机、多级缓存、熔断降级 | 缓存整体挂掉时 DB 一定扛不住，必须有**限流保护 DB**，而不是指望 DB 硬扛 |

**缓存与 DB 的一致性**（这是真正的深水区）：

- **推荐：Cache Aside + 先更 DB 再删缓存**（不是「更新缓存」）。
  删除比更新简单，且避免并发更新导致的顺序错乱。
- **仍有极小概率不一致**：读请求读到旧值 → DB 更新 → 删缓存 → 读请求把旧值写回。
  解法是**延迟双删**（更新后过一会儿再删一次）或者**订阅 binlog 删缓存**（更可靠）。
- **不要用「先删缓存再更 DB」**：删完到更新完这段窗口，任何读都会把旧值加载回来，
  概率比前者大得多。
- **业务上要接受「缓存一定会有不一致窗口」**，所以**强一致的字段不要走缓存**
  （余额、库存的真实值），缓存的是可容忍陈旧的数据。

**其他坑**：

- **大 key / 热 key**：单个 key 几 MB 会阻塞 Redis 单线程；热 key 会打爆单个分片。
  解法分别是拆分和「key 加随机后缀分散到多个副本」。
- **本地缓存的一致性更难**（多台机器各存一份，没法统一删）—— 只用于变更极少的配置类数据，
  且要有广播失效机制。
- **缓存预热**：新上线 / 重启后缓存是空的，冷启动瞬间可能压垮 DB。

### 4. 限流、削峰、熔断、降级：四个不同的东西

面试里经常被混着说，能区分开就是加分项：

- **限流**：控制入口速率，超出的**拒绝**。算法：固定窗口（有临界问题）、滑动窗口、
  漏桶（恒定速率）、**令牌桶**（允许突发，最常用）。
- **削峰**：超出的**不拒绝，排队**（MQ、队列、异步化）。适合能异步的场景（下单、发消息）。
- **熔断**：下游持续失败时**主动断开**一段时间，避免线程被拖死、避免雪崩传导。
  三态：关闭 → 打开 → 半开。
- **降级**：**主动放弃非核心功能**保核心（关掉推荐、关掉评论数、返回兜底数据）。

**坑与追问**：

- **限流限在哪一层**？网关（粗、保护整体）、应用（细、按接口）、
  用户维度（防单个用户刷）、资源维度（保护 DB 连接池）。真实系统是多层的。
- **分布式限流**要用 Redis 做计数（Lua 保证原子），但会引入一次网络调用 ——
  超高频接口一般用「本地限流 + 定期同步配额」。
- **限流的返回码和文案很重要**：返回 429 且带 `Retry-After`，客户端才知道怎么退避。
  返回 500 会导致客户端疯狂重试，把限流变成放大器。
- **重试必须带指数退避 + 抖动**。所有客户端同时在 1 秒后重试 = 第二波洪峰（惊群）。
- **熔断的阈值不好定**：绝对值（失败数）在低流量时误判，比例（失败率）在低流量时抖动大 ——
  要设最小请求数门槛。

### 5. 分库分表与分布式 ID

**什么时候分**：单表数据量大（千万级以上）、单库写入压力大、单库容量到顶。
**能不分就不分** —— 先做垂直拆分、读写分离、归档冷数据。

**分片键的选择是全局最重要的决策**，选错了改起来是灾难：

- 要满足**绝大多数查询都带上它**（否则要扫全部分片）。
- 要**分布均匀**（用户 ID 一般行，商户 ID 往往严重倾斜 —— 大商户占一半流量）。
- 常见冲突：订单表按「用户 ID」分，用户查自己的订单很快，但**商家查自己的订单要扫全表**。
  解法是**双写一份按商家维度分片的索引表**，或者把查询需求丢给 ES / 数仓。

**扩容**：一致性哈希 / 预分片（一上来就分 1024 个逻辑分片，映射到 N 个物理库，
扩容只搬分片不改路由规则）。**预分片是更常见的工程选择。**

**分布式 ID**：

| 方案 | 优点 | 缺点 |
| --- | --- | --- |
| UUID | 简单、无中心 | 无序（B+ 树页分裂严重）、占空间大 |
| 数据库号段 | 有序、简单、可用性靠双 buffer | 依赖 DB |
| **雪花算法 Snowflake** | 趋势递增、本地生成、高性能 | **依赖时钟，回拨会重复** |
| Redis INCR | 简单 | 单点、持久化时可能丢号段 |

**Snowflake 的时钟回拨**是高频追问：小幅回拨等待追平，大幅回拨拒绝服务并报警，
或者用「上次时间戳 + 逻辑自增」的变体。机器 ID 的分配也要有中心（ZK / 配置中心）。

**其他坑**：分页（跨分片的 `LIMIT 10000, 10` 是灾难，要用游标 / 上次最大 ID）、
排序、聚合、`COUNT(*)`、跨分片事务、分布式 JOIN —— **这些在分表后基本都要重新设计**。

### 6. 分布式锁：能不用就不用

- **Redis 实现的必备三点**：value 存唯一标识（防止误删别人的锁）、
  设过期时间（防死锁）、**释放时用 Lua 保证「判断 + 删除」原子**。
- **过期时间怎么定**：太短业务没做完锁就没了（两个人同时在临界区），
  太长故障时阻塞太久。工程解法是**看门狗自动续期**（Redisson）。
- **Redlock 有争议**（Martin Kleppmann 的批评：GC 停顿和时钟漂移下不安全）。
  面试提到「知道它有争议、强正确性场景我会用 DB 唯一索引 / etcd / ZK」是加分的。
- **更该说的一句**：「大多数场景我会优先用数据库的唯一约束或乐观锁把并发问题消掉，
  而不是加锁。锁是最后手段。」

### 7. 消息队列：解耦、削峰、异步

**四个必答问题**：

- **消息丢失**：生产端（发送确认 + 本地消息表）、Broker 端（持久化 + 多副本）、
  消费端（**处理完再 ACK**，不要自动提交 offset）。
- **重复消费**：一定会发生（网络、rebalance、重试），**消费端必须幂等**。
  不要试图让 MQ 保证 exactly-once —— 端到端的 exactly-once 是靠幂等实现的。
- **顺序**：全局有序几乎不可行（等于单分区单消费者）。做法是**按 key 分区局部有序**
  （同一个订单的消息进同一个分区）。注意消费端多线程会打乱顺序 —— 要按 key 分发到线程。
- **堆积**：先判断是消费慢还是生产暴涨。手段：扩消费者（受分区数限制）、
  提高单条处理效率、临时转储到另一个 topic 后面慢慢处理、丢弃可丢的。

**坑**：

- **消息里不要放大对象，放 ID**。消息发出时的数据可能已过期，消费时应回查最新状态。
- **消费失败的重试次数要有上限 + 死信队列**，否则一条毒消息能卡死整个分区。
- **消息的时序和业务的时序不一致**：「创建订单」和「取消订单」两条消息可能乱序到达 ——
  消费端要用状态机 + 版本号防止旧消息覆盖新状态。
- **MQ 不是数据库**，别把它当存储用；也别指望它有事务。

### 8. 状态机、超时补偿、延迟队列

**几乎所有涉及流程的系统（订单、支付、审核、任务）本质都是状态机。**

- **把状态和流转画出来**，比画架构图更能说明你想清楚了。
- **每一个非终态都必须有超时处理**。「待支付」超时要关单，「处理中」超时要查询或补偿。
  **一个没有超时兜底的中间态，一定会在生产上堆积。**
- **状态流转必须用条件更新**：`UPDATE ... SET state='已支付' WHERE id=? AND state='待支付'`，
  靠影响行数判断，不要「先查再改」。
- **延迟任务的实现**：
  - 定时扫表（简单可靠、有延迟、数据量大时要注意索引和分片扫描）—— **最常见也最被低估**
  - MQ 的延迟消息 / 死信 TTL
  - Redis ZSet（score 存到期时间戳，轮询取）
  - 时间轮（高性能、内存态、重启会丢）

**坑**：扫表任务在多实例部署时会重复执行（要抢占 / 分片 / 用分布式调度框架）；
扫表的 SQL 一定要有索引和 LIMIT，别一次捞 10 万条；补偿任务本身要幂等。

### 9. 热点与数据倾斜

- **热点读**：多级缓存（本地 + Redis）、key 打散到多副本、CDN。
- **热点写**：这是最难的。手段是**分桶**（把一个库存拆成 100 份分别扣，扣完再合）、
  **合并写**（一段时间内的写请求合并成一次批量更新）、**排队串行化**。
- **数据倾斜**：分片键选得不好（按商户、按城市），少数分片承载大部分流量。
  解法是复合分片键、单独给大客户开分片、二级散列。

**坑**：热点是**动态**的，昨天的热 key 今天不热。需要有**实时热点探测**（统计 + 上报），
而不是写死一份名单。

### 10. 读写分离与主从延迟

- **主从延迟是必然的**，几十毫秒到几秒。
- **经典 bug**：写完立刻读，从库还没同步，用户看到「提交成功但列表里没有」。
- **解法**：写后一段时间内强制读主（按用户维度打标）、关键读走主库、
  客户端带上写入的位点（GTID / LSN）等待同步。
- **不要用「加个 sleep」解决**，那只是把概率降低。

### 11. 可观测性：面试少人讲，但特别加分

- **三件套**：Metrics（趋势和报警）、Logging（细节和排查）、Tracing（跨服务的一次请求）。
- **必须有 traceId 全链路透传**，包括异步任务和 MQ 消息里。
  没有 traceId 的分布式系统等于没法排查。
- **看 P99 不看平均值**。平均 50ms 可能掩盖了 1% 的请求 5 秒。
- **报警要可行动**。「CPU > 80%」不可行动，「下单成功率跌破 99%」可行动。
  报警太多等于没有报警（狼来了，值班的人会全部静音）。
- **业务指标比技术指标更早暴露问题**：订单量突然跌 30%，比任何技术指标都先说明出事了。

### 12. 灰度、回滚与数据迁移

- **上线三板斧**：可灰度、可监控、可回滚。**任何不能回滚的变更都要特别对待。**
- **数据库变更不能简单回滚**，所以要遵守**先加后删、先兼容后清理**：
  1. 加新字段（可空、有默认值），新老代码都能跑
  2. 发布双写代码
  3. 刷历史数据
  4. 切读到新字段
  5. 下线旧字段的写
  6. （很久以后）删旧字段
- **加索引 / 改大表结构会锁表**，大表要用在线 DDL 工具（gh-ost、pt-osc）或者在低峰做。
- **开关（Feature Flag）要能热生效**，且要有默认值 —— 配置中心挂了要能降级到本地默认。
- **迁移必须能双跑校验**：新老逻辑同时算，比对结果，不一致时报警但以老逻辑为准，
  跑一段时间没差异再切换。这是最安全的迁移方式。

---

## 三、高频题逐个拆

每题的结构：**一句话本质 → 核心矛盾 → 必答点 → 数据模型 → 深水区 → 高频追问 → 常见错误答案**。

### 3.1 秒杀 / 限时抢购

**本质**：把 100 万个请求收敛成 100 个成功的写操作。

**核心矛盾**：瞬时超高并发（读多写少、写全部集中在极少数商品上）+ 绝对不能超卖 +
系统不能被打挂。

**必答点（层层过滤，像漏斗）**：

1. **前端**：按钮点击后置灰、答题 / 验证码打散请求、静态资源全部 CDN。
2. **接入层**：限流（按 IP、按用户、按接口总量）、恶意用户识别、非法请求直接拒。
3. **应用层**：**库存预热到 Redis**，用 Lua 脚本原子扣减（判断 + 扣减 + 记录用户在一个脚本里，
   顺便做「一人一单」去重）。**卖完立即打本地标记，后续请求不再访问 Redis**（内存标记）。
4. **异步下单**：Redis 扣减成功后发 MQ，下游慢慢创建订单、扣 DB 库存。
   用户看到的是「排队中」，前端轮询结果。
5. **DB 兜底**：`UPDATE stock SET count = count - 1 WHERE id = ? AND count > 0`，
   靠影响行数保证不超卖（**这是最后一道防线，即使 Redis 出错也不会超卖**）。
6. **超时未支付关单**并**回补库存**（Redis 和 DB 都要补）。

**深水区**：

- **Redis 扣成功了，MQ 发送失败了怎么办？** → 本地消息表 / 事务消息，或者
  用「Redis 里记录了扣减流水」+ 定时对账把丢失的补回来。
- **Redis 挂了怎么办？** → 降级到 DB 限流扣减（吞吐低但不超卖），或者直接停止活动。
  **不能降级成「不检查库存」**。
- **一人一单怎么保证？** → Redis 的 SET 记录已购用户（和扣减放同一个 Lua 脚本），
  DB 层面「用户 ID + 活动 ID」建唯一索引兜底。
- **热 key 问题**：一个商品的库存 key 会打爆单个 Redis 分片。
  → **库存分桶**：拆成 10 个 key 各 10 个库存，请求随机落一个；
  某个桶空了要能「借」其他桶（否则会出现「还有库存但用户买不到」）。
- **超时关单的库存回补要幂等**，否则重复回补会导致「凭空多出库存」（比超卖更难查）。

**高频追问**：

- 「用户点了但没抢到，怎么让他知道？」→ 前端轮询 / WebSocket / 长轮询，
  要有明确的失败终态，不能永远「排队中」。
- 「怎么防黄牛脚本？」→ 设备指纹、行为分析、风控前置、活动前不暴露真实接口路径、
  加签名和时间戳。但要承认**技术挡不住所有黄牛**，这是攻防不是一劳永逸。
- 「预扣的库存什么时候释放？」→ 延迟队列 / 定时扫表，时间要和支付超时对齐。

**常见错误答案**：

- ❌ 「用分布式锁保证不超卖」—— 能对，但 5 万 QPS 下全部串行排队，系统直接雪崩。
  锁不是秒杀的答案。
- ❌ 「直接用数据库事务扣减」—— 单行的行锁会成为瓶颈，且大量事务等待会耗尽连接池。
- ❌ 只说了不超卖，**没说少卖**。Redis 扣了 DB 没扣、消息丢了，会导致库存少卖，
  这是真实的资损（虽然不是资金损失）。**要有对账把少卖的补回去。**

### 3.2 支付系统

**本质**：在不可靠的网络上，让两方（或多方）对「钱的状态」达成一致。

**核心矛盾**：钱绝对不能错 + 依赖外部渠道（不可控、会超时、会重发）+ 必须可追溯可对账。

**必答点**：

1. **单据模型**：至少三张表 —— **支付单**（我方发起的一次支付意图）、
   **渠道流水**（与第三方的一次交互）、**账务流水**（记账，复式）。
   订单和支付单**必须分开**：一个订单可能多次支付（失败重试、部分支付）。
2. **状态机**：待支付 → 支付中 → 成功 / 失败 / 关闭。**只进不退**，成功和失败是终态
   （退款是**新的反向单据**，不是把状态改回去 —— 这一点非常关键）。
3. **幂等**：商户订单号唯一索引；同一个订单号重复发起返回第一次的结果。
4. **回调处理**：第三方回调**一定会重复、可能乱序、可能丢失**。
   - 验签（必须）
   - 幂等（必须）
   - **金额校验**（回调里的金额必须和本地单据一致，不一致直接报警 —— 防篡改）
   - 处理完再返回成功，别先返回后处理
5. **主动查询兜底**：不能只依赖回调。要有**定时轮询**未终态的单据去查第三方
   （典型策略：1s、5s、15s、30s、1m、2m、5m…递增查询）。
6. **对账**：T+1 拉第三方账单，与本地流水逐笔比对。
   差异分四类：**我有它无、它有我无、金额不符、状态不符**。每类都要有处理流程。
7. **复式记账**：每笔资金变动记借贷两条，保证「借贷必相等」。
   有了这个才能做**试算平衡**（全量校验账不会错）。

**深水区**：

- **「支付中」是最危险的状态**。用户看到「处理中」，钱可能已经扣了。
  所有中间态都必须有超时和最终裁决。**长时间处于中间态的单必须报警到人。**
- **掉单**：用户付了钱但我们没收到通知也没查到。靠**主动查询 + 对账**双保险。
- **重复支付**：用户在两个页面各付一次。要么用幂等挡住，要么**自动退款**（要有这条链路）。
- **金额一律用整数分（或 `Decimal`）**，绝不用浮点数。所有涉及金额的字段、
  接口、日志都统一单位并写进文档。
- **退款**：退款是独立单据，有自己的状态机，也有幂等（退款单号）、
  也可能失败、也要对账。**部分退款、多次退款**要能累加校验不超过原单金额。
- **渠道路由与降级**：多个支付渠道，某个渠道故障要能自动切换 / 摘除。
  但**已经发起到 A 渠道的单不能改走 B 渠道**（会重复扣款）。

**高频追问**：

- 「回调和主动查询同时返回了，怎么办？」→ 幂等 + 条件更新，谁先到谁生效，后到的空操作。
- 「怎么保证订单状态和账务状态一致？」→ 同库事务 or 本地消息表 + 对账兜底。
- 「余额扣减怎么防并发？」→ 乐观锁（版本号）或 `UPDATE ... WHERE balance >= ?`，
  绝不「查余额 → 计算 → 写回」。
- 「怎么做资金安全？」→ 每日试算平衡、单笔限额、异常波动报警、
  关键操作双人复核、所有变更留痕。

**常见错误答案**：

- ❌ 把订单表的 `status` 直接改来改去，没有独立的支付单和流水。
  一旦出问题**完全无法追溯发生了什么**。
- ❌ 只做回调，不做主动查询。第三方回调丢一次就是一笔资损。
- ❌ 没有对账。「我们的系统很可靠所以不需要对账」是最危险的回答。
- ❌ 退款用「把支付单状态改成已退款」实现。

### 3.3 订单与库存（电商交易链路）

**本质**：一个跨多个服务的长事务（下单 → 锁库存 → 支付 → 发货），每一步都可能失败。

**必答点**：

- **订单状态机**要完整画出来，包括各种取消路径。
- **库存的三个数**：可用库存、锁定库存、已售。下单锁定，支付后扣减，超时释放。
- **超卖 vs 少卖**：技术上更怕超卖（要赔付），但业务上少卖也是钱。
  两者都要靠对账收敛。
- **拆单**：一个订单可能拆成多个子订单（多商家、多仓库），金额分摊 /
  优惠分摊要能精确到分，**分摊的余数处理要有明确规则**（否则会差 1 分）。
- **取消/超时/退款**都要回补库存和优惠券，且必须幂等。

**深水区**：

- **优惠计算的顺序会影响金额**（先满减还是先打折），必须固定且可复现。
  订单要**快照**下单时的价格和优惠，不能实时算（商品改价会导致历史订单金额变）。
- **快照是订单系统的核心思想**：商品名、价格、地址、优惠规则全部快照。
- **分布式事务**：下单、锁库存、扣优惠券三个服务 —— 用 Saga 或 TCC，
  且要有正向重试 + 反向补偿 + 最终对账。

### 3.4 短链系统

**本质**：一个海量、读多写少、要求低延迟的 KV 映射。**最经典的入门题，考的是基本功。**

**必答点**：

- **短码生成**：
  - 发号器 + 62 进制转换（**推荐**：无冲突、可控长度、可预分配）
  - 哈希（MD5 取前几位）+ 冲突处理（要处理，且冲突随数据量增长）
  - **不要用自增 ID 直接暴露**（可被遍历爬取全部链接），可以做混淆映射。
- **存储**：KV 存储 / 分库分表的 MySQL，短码为分片键。数据量大但结构简单。
- **读**：几乎全部走缓存，命中率可以做到很高（长尾也可以用布隆过滤器挡掉不存在的）。
- **重定向用 301 还是 302**？**302**（临时）—— 301 会被浏览器永久缓存，
  你就**再也统计不到点击量**，也没法下线链接。这是这题的经典考点。

**深水区与坑**：

- **同一个长链接重复提交要不要返回同一个短码**？要看业务：如果要分别统计来源就不能复用。
- **过期与删除**：过期后短码能不能复用？复用会导致老链接跳到新目标（安全问题），
  一般不复用。
- **恶意链接**：短链是钓鱼的天然工具，需要黑名单 / 安全扫描 / 举报机制。
- **点击统计**要异步（打日志 / 发 MQ），绝不能同步写 DB 拖慢跳转。
- **自定义短码**要防冲突、防敏感词、防抢占。

### 3.5 Feed 流 / 朋友圈 / 微博时间线

**本质**：多对多的内容分发，核心是**读扩散 vs 写扩散**的取舍。

| 方案 | 怎么做 | 优点 | 缺点 |
| --- | --- | --- | --- |
| **拉模式（读扩散）** | 读时实时查所有关注人的最新内容并归并 | 写入轻、存储小、无延迟 | 读放大严重、关注多时慢 |
| **推模式（写扩散）** | 发布时写进所有粉丝的收件箱 | 读极快 | **大 V 发一条要写千万次**、存储放大 |
| **推拉结合** | 普通用户推，大 V 拉；活跃用户推，不活跃的拉 | 平衡 | 复杂、要维护两套逻辑 |

**深水区**：

- **大 V 问题是这题的核心**。一个千万粉丝的账号发帖，写扩散要写千万条 ——
  必须异步 + 分批 + 限速，且要接受延迟（几分钟内投递完）。
- **不活跃用户不推**（80% 的收件箱永远没人看），登录时再拉。这能省掉大部分写入。
- **分页要用游标（时间戳 / ID）不能用 offset** —— Feed 是动态的，
  offset 分页会漏内容或重复。
- **排序**：纯时间序简单；一旦引入算法排序，就需要召回 + 排序两阶段，是另一个系统了。
- **取关 / 删除 / 屏蔽**后收件箱里的数据怎么清理？一般**读时过滤**而不是写时删除。

### 3.6 IM / 即时通讯

**本质**：可靠的、有序的、低延迟的消息投递 + 海量长连接管理。

**必答点**：

- **长连接**：WebSocket，接入层要能维护百万级连接（连接数 × 内存、
  文件描述符上限、心跳保活）。
- **路由**：用户 ↔ 接入机的映射存 Redis，发消息时查对方在哪台机器上。
- **消息 ID**：**必须单会话内严格递增**（用于排序和补齐），一般用「会话内序号」而不是全局 ID。
- **可靠投递**：发送 → 服务端落库 → 回 ACK 给发送方 → 推给接收方 → 接收方 ACK。
  **少一个 ACK 就会丢消息或重复。**
- **离线消息**：接收方不在线时存离线表，上线后拉取；要有数量上限和过期。
- **多端同步**：一条消息要投递到用户的所有设备，且已读状态要同步。
  **用「同步点位（seq）」模型**：客户端上报自己的最大 seq，服务端返回之后的所有消息。
  这个模型比「推消息」健壮得多。

**坑**：

- **顺序**：网络乱序 + 多端 + 重发，客户端必须按 seq 排序和去重，不能按到达顺序显示。
- **群消息扩散**：500 人群一条消息 = 500 次投递。大群要做「读扩散」（群消息只存一份，
  各成员维护自己的已读位点）。
- **已读回执**在大群里是 N² 的写入量，一般大群不做或者只做数量。
- **心跳间隔**：太短耗电耗流量，太长发现断连慢。要和移动网络的 NAT 超时配合（一般 3~5 分钟）。

### 3.7 排行榜 / 计数系统

- **Redis ZSet** 是标准答案（`ZADD` / `ZREVRANGE` / `ZRANK`）。
- **深水区**：
  - 亿级用户全量排名，ZSet 内存吃不消 → 分桶（按分数段分片）+ 二次归并求近似排名。
  - **相同分数怎么排**？用「分数 × 10^n + (最大时间戳 - 时间戳)」把时间编进 score，
    保证先达到的排前面。
  - **实时性要求**：全量实时排名往往不必要，「前 100 名实时 + 自己的排名近似」够用。
- **计数（点赞、播放量）**：
  - 高频写要**先在 Redis 累加，定时落库**（能承受少量丢失时）。
  - 精确计数用 DB 的原子更新，注意热点行。
  - **去重计数（UV）用 HyperLogLog**（有误差、内存极省）或 bitmap（精确、按用户 ID）。

### 3.8 优惠券 / 红包

- **发放**：批量发券要异步（一次发几百万张）；库存控制同秒杀。
- **核销**：必须幂等，且「锁定 → 使用 → 释放」要有超时。
- **红包拆分**：**发的时候一次性拆好存起来**（简单、可预测、可对账）
  或**抢的时候实时拆**（二倍均值法，内存态需要考虑并发）。
  一般选前者，因为可对账。
- **坑**：
  - 优惠券和支付必须在**同一个事务边界或同一套补偿链路**里，
    否则会出现「券核销了但订单没成」。
  - **过期券的处理**要有定时任务，且过期后不能再被使用（校验用券时判过期，不能只靠定时任务）。
  - 金额分摊到多个商品时的**取整余数**，规则必须固定（一般给第一个 / 最大的那个）。

### 3.9 通知与推送系统

- **多通道**：站内信、Push、短信、邮件、微信 —— 抽象成统一的「通知任务」+ 通道适配。
- **必答点**：模板管理、变量渲染、多语言、频次控制（防打扰）、
  用户偏好（可退订）、免打扰时段、失败重试、送达回执。
- **坑**：
  - **第三方通道的限速和配额**（短信一秒几百条），必须有本地队列削峰。
  - **重复推送**是用户投诉最多的问题 —— 幂等键要做到「同一个业务事件只推一次」。
  - **设备 token 会失效**，收到 410 / 无效 token 要立即清理订阅
    （本项目 `PushNotifier` 就是这么做的）。
  - 通知内容里**不要放敏感信息**（锁屏可见）。

### 3.10 限流器（单独作为一题被问）

- 实现四种算法，说出各自的临界问题：
  - **固定窗口**：窗口边界处可能出现 2 倍流量。
  - **滑动窗口**：解决临界问题，代价是要存更多数据（日志法内存大，格子法有精度取舍）。
  - **漏桶**：恒定流出速率，不允许突发，会导致排队延迟。
  - **令牌桶**：允许一定突发，最常用。
- **分布式实现**用 Redis + Lua（原子），或者「预分配配额到各节点 + 定期再平衡」。
- **坑**：限流的计数本身也可能成为热点；Redis 挂了要 fail-open 还是 fail-close？
  （保护型限流一般 fail-open 保可用，防刷型限流 fail-close 保安全 —— **这是个业务决策**。）

### 3.11 大文件上传 / 网盘

- **分片上传 + 断点续传 + 秒传**：
  - 秒传 = 先算文件哈希，服务端存在就直接建引用（**注意哈希碰撞和「不同用户同一文件」的权限**）。
  - 分片上传 = 客户端切块并发上传，服务端记录已收分片，最后合并。
  - 断点续传 = 上传前先查已上传的分片列表。
- **直传对象存储**：客户端向服务端要**预签名 URL**，直接传 OSS/S3，
  文件流不经过应用服务器（否则带宽和内存都扛不住）。
- **坑**：
  - 分片的**清理**（上传一半不传了，垃圾分片要定时清）。
  - 合并是耗时操作，要异步 + 状态查询。
  - **回调可信性**：对象存储的上传回调要验签，且要**服务端再校验一次文件大小和类型**，
    不能信客户端说的。
  - 大文件的下载要支持 **Range 请求**（断点续传、拖进度条）。

### 3.12 附近的人 / 打车派单

- **地理索引**：GeoHash（把二维降成一维字符串，前缀相同即接近）或 Redis GEO（底层就是 GeoHash + ZSet）。
- **坑**：
  - **GeoHash 的边界问题**：两个很近的点可能前缀完全不同（跨格子），
    必须**查询周围 8 个格子**。
  - 精度和格子大小的取舍：格子太大要过滤太多，太小要查太多格。
  - 城市密度差异巨大：市中心一个格子几万人，郊区一个格子零人 —— 需要动态半径扩大。
- **派单**是另一个问题：不是最近优先，而是全局最优匹配（考虑接单率、方向、订单价值），
  一般用批量撮合（每 2 秒撮合一次）而不是来一单派一单。

### 3.13 定时任务调度系统

- **单机 cron 的问题**：单点、无法扩展、无失败重试、无可观测。
- **分布式调度**要解决：**同一个任务只执行一次**（选主 / 抢占 / 分片）、
  失败重试、分片执行（一个大任务拆给多台机器）、任务依赖、错过的执行怎么补（misfire 策略）。
- **坑**：
  - **任务执行时间超过调度间隔**会导致并发执行 —— 必须有「上次没跑完就跳过」的保护。
  - **服务器时钟不同步**会导致重复或漏执行。
  - 大批量扫表任务要分片 + 限速，否则一个报表任务能把主库拖垮。
  - **发布重启时正在执行的任务**怎么办？要有优雅停机和续跑能力。

### 3.14 评论 / 树形结构

- **存储**：邻接表（parent_id，查子树要递归）、
  路径枚举（`path = "1/5/23"`，查子树用前缀 LIKE，**最常用**）、闭包表（空间换时间）。
- **一般只做二级**（评论 + 回复），因为无限层级的 UI 和查询都很难做。
- **坑**：删除父评论时子评论怎么办（一般软删 + 显示「该评论已删除」）；
  楼中楼的分页；热度排序和时间排序的切换；计数的一致性。

---

## 四、做真实项目一定会踩的坑（分类清单）

这一节是「项目经验」，面试里能自然带出来就非常加分，因为**背不出来**。

### 4.1 金额与数值

- **绝不用浮点数存金额。** 用整数分或 `DECIMAL`。`0.1 + 0.2 != 0.3` 会真的变成资损工单。
- **单位要统一并写进接口文档**：分 / 元 / 厘。跨系统对接时单位不一致是经典事故。
- **除法和分摊必有余数**，规则要固定（尾差给谁），且要有校验「分摊之和 == 总额」。
- **汇率、税率、折扣**的计算顺序和舍入方式要固定，且要能复现历史计算。

### 4.2 时间与时区

- **数据库存 UTC，展示时转本地时区。** 混着存必然出事。
- **跨天的定义是业务定义**：「今天的订单」在凌晨 2 点算哪天？用户时区还是服务器时区？
- **夏令时**：某些时区一年会有一个小时不存在、一个小时重复 ——
  定点任务在这两个小时可能不执行或执行两次。
- **前端传来的时间不可信**（用户能改手机时间），关键时间戳以服务端为准。
- **NTP 时钟回拨**会影响 Snowflake、限流窗口、缓存过期、分布式锁。
- **本项目相关**：`CheckInPlan` 的静音时段是**按用户时区**算的、还能跨午夜，
  就是这类坑的具体实例。

### 4.3 并发与事务

- **「先查后改」几乎总是错的**，要用条件更新 / 乐观锁 / 唯一索引。
- **事务里不要有 RPC、HTTP 调用、发消息、发邮件**：外部调用慢会长时间占着数据库连接和行锁，
  连接池耗尽 → 全站雪崩。**这是最常见的生产事故之一。**
- **事务要尽量短**，把耗时操作挪出事务边界。
- **注意事务的传播行为和自调用失效**（Spring 的 `@Transactional` 自调用不生效，
  Rails 的嵌套 `transaction` 默认不开真正的 savepoint）。
- **死锁**：多个事务以不同顺序更新同一批行。解法是**统一加锁顺序**（比如永远按 ID 升序）。
- **批量操作**（`IN (...)` 几千个 ID、批量 update）会长时间持锁，要分批。

### 4.4 数据库

- **没有索引的查询在测试环境很快**（数据少），上线后拖垮主库。上线前必须 `EXPLAIN`。
- **索引不是越多越好**：拖慢写入、占空间、可能选错索引。
- **隐式类型转换会让索引失效**（varchar 字段传数字）。
- **大表的 `COUNT(*)`、`OFFSET` 深分页**是性能杀手。深分页用「上次最大 ID」游标。
- **`SELECT *`** 在有大字段（text / blob）的表上会拖垮网络和内存。
- **软删除**（`deleted_at`）会让所有查询都要带条件，忘一个就是数据泄露；
  也会让唯一索引失效（删了再建同名会冲突）—— 解法是唯一索引带上 deleted 标记。
- **枚举值用整数还是字符串**：字符串可读、便于排查；整数省空间。
  但**绝不要用数据库的 ENUM 类型**（加值要改表结构）。
- **NULL 的语义**：`NULL != NULL`，`NOT IN` 遇到 NULL 返回空集，`COUNT(col)` 不算 NULL。
- **外键**在高并发下会带来额外锁，很多大厂禁用外键，靠应用保证 —— 代价是会有脏数据，
  要有定期校验。

### 4.5 缓存（工程侧）

- **缓存 key 要有统一的命名规范和版本号**，改了数据结构要能整体失效。
- **缓存穿透到 DB 的兜底是限流**，不是「多加几台 DB」。
- **序列化格式变更**会导致反序列化失败 —— 新老版本共存期间要能容错。
- **删缓存失败要有补偿**（重试队列 / binlog 订阅）。
- **本地缓存 + 分布式缓存双层**时，本地缓存的失效最难保证，尽量只缓存极少变的数据。

### 4.6 消息队列（工程侧）

- **消费端幂等是必须的**，不是可选的。
- **消息积压时不要盲目重启消费者**（会重复消费大量消息）。
- **顺序消费 + 多线程**必须按 key 分发。
- **消息体里放 ID 不放全量数据**，消费时回查。
- **本地消息表 / Outbox 模式**是解决「写库 + 发消息」原子性的标准做法，
  比事务消息更容易在任意技术栈实现。

### 4.7 第三方对接

- **一定会超时、一定会返回你没见过的错误码、一定会在你不知道的时候改接口。**
- **必须设置连接超时和读超时**（不设默认可能是无限，一次故障就打满线程池）。
- **必须有熔断和降级**，且降级后的业务语义要明确（当作失败还是当作待定？
  支付场景绝不能当作失败）。
- **所有请求和响应都要记日志**（脱敏后），出问题时这是唯一的证据。
- **沙箱和生产的行为不一致**是常态，上线前要有真实小额验证。
- **对方的回调 IP 白名单、验签、重放防护**都要做。
- **对方的文档往往是错的**，以实际返回为准，并把实际返回记录下来。

### 4.8 上线与运维

- **配置和代码分离**，但**配置变更同样要灰度和回滚**（改错一个配置的破坏力不输改错代码）。
- **发布时的新老代码共存**：接口要向前向后兼容，
  新代码不能依赖「所有实例都已升级」的假设。
- **数据库迁移和代码发布要解耦**（先加字段发布 → 再发布用它的代码）。
- **不要在业务高峰发布**，也不要在周五下班前发布。
- **日志要有分级和采样**，全量 DEBUG 日志能把磁盘写满（磁盘满 = 全站挂）。
- **磁盘、连接数、文件句柄、线程池**这些「配额型资源」的耗尽是最隐蔽的故障。
- **压测要压到崩溃点**，只压到「符合预期」不知道系统的边界在哪。
- **要有一键降级开关**，故障时第一动作是止血，不是排查。

### 4.9 安全

- **越权是最常见的漏洞**：查询别人的订单只要改 ID。
  **每个接口都要校验「这条数据属不属于当前用户」。**
- **敏感信息脱敏**：日志、监控、报错页面里不能出现手机号、身份证、token。
- **重放攻击**：关键接口要有时间戳 + nonce + 签名。
- **枚举攻击**：自增 ID 暴露业务量，也便于爬取。
- **限流要有用户维度**，否则一个账号就能刷爆。
- **不要信任任何客户端传来的东西**，包括价格、状态、权限标记
  （本项目 `CheckIn` 的答案必须过 `OPTIONS` 白名单就是这条）。

### 4.10 测试与可维护性

- **写一个能复现的失败用例再修 bug**，否则你不知道有没有修好。
- **测试里不要真的调外部服务**（本项目的规矩是用 `stubbing` 顶掉 `Assistant`）。
- **并发 bug 很难写测试**，能做的是把并发点收敛到少数几个地方（唯一索引、条件更新），
  让它们可测。
- **时间相关的逻辑要能注入时间**，否则测不了「超时」和「跨天」。
- **幂等要有测试**：同一个请求跑两遍，断言结果一致且副作用只发生一次。

---

## 五、表达层面的套路（同样是套路，但很多人不练）

### 主动说、不等问的加分句式

- 「我先确认一下规模和一致性要求，因为它决定我要不要引入 X。」
- 「这个方案的代价是 Y，我接受它是因为我们更怕 Z。」
- 「这里有个失败路径：如果 A 成功但 B 失败，会出现……，所以我需要 C 来兜底。」
- 「这个量级其实还不需要分库分表，我会先做 X，等到 Y 的时候再考虑。」
- 「如果这是我真的要上线的系统，我会先上简单版本 + 监控，观察真实分布再优化。」

### 反模式（面试官心里在扣分）

| 反模式 | 为什么扣分 |
| --- | --- |
| 不问规模直接开画 | 说明没有「按约束设计」的意识 |
| 技术名词堆砌（Kafka + K8s + Flink） | 说明没想清楚为什么，且暴露过度设计倾向 |
| 只讲 happy path | 说明没上过线 |
| 每个方案都说「都可以」 | 面试就是考取舍，不取舍等于没答 |
| 一被质疑就立刻改口 | 说明原方案不是想出来的。**该坚持时要能说出理由** |
| 全程「一般来说」「通常」 | 说明没做过 |

### 讲自己的项目：三个数字 + 一个取舍 + 一个故障

比 STAR 更实用的模板：

1. **规模数字**：QPS 多少、数据量多大、多少人用、成本多少。没有数字的项目听起来像玩具。
2. **一个真实取舍**：「我们选了 A 没选 B，因为……，代价是……，后来证明……」。
3. **一个真实故障**：出了什么事、怎么发现的（说明有监控）、怎么止血的（说明有预案）、
   怎么根治的（说明会复盘）。**讲故障是最高效的可信度证明。**

反过来，**不要说「我负责的模块从来没出过问题」** —— 要么系统没流量，要么你不知道它出过问题。

---

## 六、把这些落到 kacam 的问题池

这批内容天然适合问题池的形态：**一个真实的问题 + 一份把它聊透的指引**。
但要注意本项目问题池现有的结构和边界。

### 现状

- 问题池是两级：**范畴（`Realm`）→ 分类（`Topic::CATEGORIES`）→ 问题（`Topic`）**。
- 目前只有两个范畴：**做人**（住在 `/topics`）和**做产品**（`/topics/product`）。
- 范畴不落库，从 `topic.category` 反查（`Realm.of`），所以**分类键必须全局唯一**。
- 每个范畴有自己的一套 system prompt（`Assistant::CHAT_SYSTEM_PROMPTS`，键就是范畴键）。

### 如果要新开一个「做技术」范畴，需要动的地方

按 AGENTS.md 里「加一个范畴要动的地方」那一节：

1. `Realm::ALL` 加范畴，定好分类键（**注意和现有键不能重名**，
   比如「心态」已经被做产品占了，技术侧要用别的键名）。
2. `Assistant::CHAT_SYSTEM_PROMPTS` 补一套人格 —— 陪人聊「该不该回老家」和
   陪人聊「这个分片键选得对不对」不是一件事。
3. 12 个 locale 文件里的 `topics.realms.*` 和 `topics.categories.*`。
4. `db/seeds.rb` 里的内容。

这是个需要确认的改动（新增范畴属于产品决策），**先别动代码**。

### 分类的候选划分

按这份文档的结构，比较自然的分法是：

| 候选分类 | 覆盖本文的 |
| --- | --- |
| 系统设计 | 第一、三节（骨架 + 各类题） |
| 分布式与一致性 | 第二节的 1、2、6、7 |
| 性能与容量 | 第二节的 3、4、9、10 |
| 数据与存储 | 第二节的 5，第四节的 4.3、4.4 |
| 线上与故障 | 第二节的 11、12，第四节的 4.8 |
| 工程习惯 | 第四节的其余部分 |

### `guidance` 该怎么写（这批题的特殊之处）

问题池的 `guidance` 会**原样进 system prompt**，决定这个话题下 AI 怎么陪聊。
技术类话题有一个和「做人」「做产品」不同的风险：**AI 很容易直接给出标准答案，
把「聊透一个问题」变成「背一遍八股」。** 那就失去问题池的意义了，
用户直接问模型就好，不需要这个产品。

所以这类 `guidance` 的重点应该是：

- **先问对方的真实场景和真实规模**，没有规模就没有设计。
- **逼出取舍和代价**，对方说「用 Redis」就追问「Redis 挂了呢」「一致性怎么保证」。
- **区分「他知道」和「他做过」**：问具体的数字、具体的故障、具体的妥协。
- **不给标准答案，给追问**。这和解梦那条「给角度不给答案」是同一个形状 ——
  **AI 只出素材，判断是人的**（对应 `Post#takeaway` 强制作者自己写结论）。
- 明确不做的事：不评价对方的技术水平、不预测他能不能过面试、不替他做技术选型决策。

### 内容的语言

`Topic` 只做了中英两套，其他语言回退到英文（`Topic.display_locale`）。
这批技术话题如果要出英文版，`guidance` 必须人工写，**不能机翻** ——
机翻的指引会直接拖垮对话质量。

---

## 附：一句话速查表

| 场景 | 第一反应 |
| --- | --- |
| 任何写接口 | 幂等键 + 唯一索引 |
| 状态流转 | 条件更新 `WHERE state = ?` + 看影响行数 |
| 任何中间态 | 一定要有超时兜底 |
| 涉及钱 | 独立单据 + 流水 + 主动查询 + 对账 |
| 涉及库存 | 预扣 + 超时释放 + DB 兜底条件 + 对账 |
| 高并发读 | 多级缓存 + 热 key 打散 |
| 高并发写 | 异步削峰 + 分桶 + 合并写 |
| 跨服务一致性 | 本地消息表 / Saga + 对账，不上 XA |
| 分库分表 | 先想分片键，再想跨片查询怎么办 |
| 消息队列 | 消费端幂等 + 死信 + 按 key 分区保序 |
| 第三方依赖 | 超时 + 重试退避 + 熔断 + 降级语义 |
| 上线 | 可灰度、可监控、可回滚；DDL 先加后删 |
| 排查 | traceId 全链路 + P99 + 业务指标报警 |
