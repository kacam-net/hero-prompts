# 练功房：系列怎么扩

> 这份是**讨论稿**。第一版写在练功房落地之前，现在按已实现的代码（`Drill` /
> `DrillSeries` / `DrillCatalog` / `DrillAttempt` / `ChatTarget`，见 AGENTS.md
> 「练功房」那节）重审了一遍。
>
> **架构那部分不在这里** —— 「加一个系列要动哪五处」是 AGENTS.md 的活，别抄第二份。
> 这份只回答：**哪些系列成立、按什么顺序做、写提示词本身要守什么。**

---

## 一、Drill 是什么（六个组件，只有一个不可省）

| 组件 | 干什么 | 现有实现 |
| --- | --- | --- |
| **角色反转** | 模型当考官/教练，不当答案库 | 「我要的不是你的标准答案 —— 那我直接问你就有了」 |
| **铁律** | 压住大模型的默认人格 | 六条：一次一问 / 不剧透 / 追代价 / 分知道与做过 / 不许夸 / 不替我决定 |
| **分幕流程** | 每幕有「你要判定的」和「通过标准」 | 六幕，45 分钟 |
| **口令** | 用户随时能换挡 | `跳过` `提示` `直说吧` `复盘` |
| **底牌** | 事先写死的判卷清单，放文件末尾，不得提前透露 | `## 底牌`：必答点 + 深水区 + 常见错答 |
| **复盘卡片** | 可存档、可跨次对比的产出 | ✅🟡❌ 覆盖表 + 代价对照 + 哪些是背的 + 三个洞 + 总评 |

**六件里只有底牌不可省。** 没有它，模型收到题面的默认行为就是把标准答案铺开讲一遍，
用户学到的是「读了一遍」，不是「自己推出来过」。

所以判断一个新系列成不成立，只有一个问题：**这件事的底牌在哪儿、长什么样。**

## 二、三种底牌形态

现有 17 份已经混了两种（14 份案例题一种，`91-your-own-project` 另一种），再往外扩会出现第三种。
**三种的铁律不一样、收尾卡片不一样、在代码里的表现也不一样。**

| 型 | 底牌在哪 | 判卷靠 | 核心铁律 | 代表 |
| --- | --- | --- | --- | --- |
| **A · 有标准底牌** | 写死在正文末尾 | 覆盖表 ✅🟡❌ | **不剧透** | 系统设计、产品设计、故障排查 |
| **B · 底牌在你身上** | 没有预设答案 | **具体性**：数字、故障、取舍 | **不接受抽象词，立刻要数字**；不要替我把答案圆回来 | 讲自己的项目、复盘、方案答辩、简历 |
| **C · 底牌是表现量表** | 维度评分表，不是知识点 | 当场表现 + 引原话当证据 | **不替我组织语言**；过程语言 = 目标语言 | 英语面试、费曼讲解、谈判 |

三型共用的只有两条：**不许夸我**、**收尾必须产出可存档的卡片**。

这个分型不是纸上的：AGENTS.md 里「说明那一段是一个系列一个 partial，不是一个字段」
给的理由就是「这个系列有六条铁律和四个口令，**下一个可能是三轮打分表**」——
那说的正是 C 型。架构上的口子已经留好了。

### 两条实现层面的约定（现在就得定死，写进去晚了要改数据）

1. **C 型的量表不要写成 `## 底牌`。**
   `Drill#answer_key?` 是靠正文里有没有 `## 底牌` 来决定页面上提不提醒
   「复制走的那段里带着判卷用的底牌，别自己先读」。A 型对，B 型没有底牌也对，
   但 **C 型的评分维度本来就该让人先看**（知道判什么维度不影响表现，反而有帮助）。
   写成 `## 底牌` 的话，页面上会劝人别读一份本该先读的东西。
   → C 型用别的小标题（`## 评分维度`），把 `## 底牌` 留给 A 型。

2. **`locale` 是素材的语言，不是练习的语言。**
   英语面试那份提示词是**中文写的指令**（内容是「全程用英语提问，我卡住才允许中文」），
   它属于 `zh-CN` 素材，不该塞进一个 `en` 的 `sources`。塞错的直接症状是
   `Drill.display_locale` 把它判成「中文界面没有这个系列」然后整个系列在中文站上消失。

## 三、扩张的真实成本：先有 playbook，才有底牌

第一版方案里我把「写 15 份提示词」当成一个系列的成本，**这是错的**。

现有 17 份的质量来自那份 55K 的
[system-design-interview-playbook.md](system-design-interview-playbook.md)：
底牌全部来自它，`system-design-cases/README.md` 明说「没有额外发挥」；
`DrillSeries::ALL` 里那份 19 个键的标签词表也是从 playbook 第二、四节抄下来的。

**所以一个 A 型系列的真实成本是：先写一份 playbook，再拆成提示词。** 拆是机械劳动，
写才是。没有 playbook 的 A 型系列，底牌是现编的，标签词表是拍脑袋的 —— 那就退化成
`00-generic-template.md` 那种「模型自己现推」的东西，判卷不准。

**而 B 型系列没有底牌，所以不需要 playbook。** 这直接改了排序：

| | A 型 | B 型 | C 型 |
| --- | --- | --- | --- |
| 前置素材 | **一份 playbook** | 无 | 一份评分量表（短） |
| 一个系列要几份提示词 | 10~15（题海才有覆盖） | **2~4 就够** | 3~6 |
| 为什么 | 每道题一份底牌 | 同一份反复做、每次换对象（`91` 自己就写了「值得反复做」） | 每种场景一份 |

**结论：B 型是最省的扩张方式，应该先扩 B 型。**

## 四、系列候选

「标签词表」和「给哪几家模型」（`ChatTarget.for_series`）是每个系列上手要先定的两件事，
所以列进表里。

### 第一批：B 型，没有前置素材，最快能上

| 系列 | 型 | 几份 | 为什么成立 | 要注意 |
| --- | --- | --- | --- | --- |
| **复盘教练** | B | 2~3（项目复盘 / 一次故障 / 一段时间） | `91-your-own-project` 的近亲，形状现成 | 复盘的失败模式是流水账和「归因到运气和别人」。铁律要加一条治归因：**每条归因都追「下次你能改的是哪一步」** |
| **技术方案答辩** | B | 1~2 | 带**自己正在做的方案**去被拷问 —— 不是练面试，是真能改进手上的东西。可能是这批里最值钱的一个 | 和 `91` 的区别要写清楚：那份挖过去，这份拷问将来 |
| **行为面试 / 简历拷问** | B | 2（STAR / 贴简历） | STAR 的 R 最容易空 | 铁律：**每条经历都问「这件事没有你会怎样」** |

### 第二批：A 型，等 playbook

| 系列 | 型 | 卡在哪 |
| --- | --- | --- |
| **产品设计陪练** | A | 和系统设计同构（约束 → 取舍 → 代价），但**要先有一份产品设计的 playbook**。没有它就别开工 |
| **线上故障排查** | A（反转） | 唯一一个「你问它答」的变体：模型给症状，你要日志、提假设，它按预设真相回答。底牌 = **一份真相 + 一串会误导人的伪线索**。铁律要改：模型只能回答问到的，**不许主动提示下一步看哪里**。素材可以从 playbook 第四节现有的坑清单里长出来，成本比产品设计低 |
| **算法题陪练** | A | 成立，但**必须窄化成「讲清楚为什么」**——刷题去 LeetCode，这里练先说思路、说复杂度、说边界、说为什么不是另一种解法。铁律要加一条：**在我说出可行解之前不许提到任何算法名词**，否则模型太容易直接甩最优解 |

### 第三批：C 型，先拿一份验证形态

**英语面试陪练**。维度：结构 / 准确 / 词汇 / 流利，四维各自给分并引原话；
收尾卡片要有「你说过的原句 → 更地道的说法」那一栏。
建议**先只做一份**，把 C 型的铁律模板和 `## 评分维度` 的约定立起来再铺开。
`ChatTarget.for_series` 那个口子在这里可能第一次用上（哪几家判英语口语靠谱是个经验问题，
先全给，用过再收）。

### 候补

指标设计（「这个指标会被怎么作弊」）、慢查询会诊、费曼讲解（C）、薪资谈判（C）、
商业点子拷问（YC 式：谁付钱、为什么现在、为什么是你）、论文答辩、前端系统设计。

### 明确不收的两类

1. **人生决策和情绪心理**（该不该换工作、要不要回老家、这段关系怎么办）。
   Drill 的形状是「拿底牌判你、不许夸你、追到你答不出为止」，人生问题没有底牌，
   硬做就是审问，而且必然滑向替人做决定 —— 撞的是「做人那个范畴的边界和解梦同一条」
   那条红线。**这类归问题池，那边的人格是陪聊不是拷问。**
2. **不需要提示词的东西**（读物、清单、速查表）。AGENTS.md 已经写死了：
   能进这个板块的只有「一份提示词，拿去别处练」这一种形状。

> 第一版里我把「带作品来」（代码 review、PRD、设计稿）也划进了不收，**这条撤回**。
> 带作品来仍然是「一份提示词拿去别处练」，只是底牌在作品上 —— 它是 **B 型的第二个亚型**：
> B 型有「带经历来」（复盘、项目）和「带作品来」（方案、简历、代码）两种，
> 上面的「技术方案答辩」就是后者。

## 五、对着实现审第一版方案：撤回两条，新增三条待办

### 撤回：目录不用搬

第一版建议把素材统一搬成 `docs/drills/<系列>/`。**撤回。**
`DrillSeries::ALL[…][:sources][locale][:dir]` 已经把目录变成了**登记项而不是约定**
（而且是故意写全路径不按约定拼的，理由在那段注释里：Brakeman）。
新系列直接在登记处写自己的目录名就行，搬现有目录只会断掉已有外链，收益接近零。

### 收窄：`_authoring.md` 只讲提示词本身

第一版说要写一份「怎么写一份新 Drill」的规格。**还是要写，但范围要收窄**：
代码侧那五步（`DrillSeries::ALL` → md → yml → partial → locale → import）
AGENTS.md 已经有了，**别抄第二份**，抄了就会两处不一致。

`_authoring.md` 只该讲 AGENTS 不管的那半边：

- 三型各自的**铁律模板**（第二节那张表的展开）
- 幕次骨架和口令表
- 收尾卡片的格式（三型各一份）
- `## 底牌` vs `## 评分维度` 的约定（第二节那两条）
- 一份新提示词上线前的自查：底牌在末尾且标注不得透露？有「不许夸我」？
  卡片能存档并和下一次对比？口令表在？分隔线以上是给读仓库的人看的、以下是可复制全文？

至于「重复太多要不要上生成器」——第一版说等第三个系列再看，这条不变。
现在只有一个系列，切在哪儿还看不出来。

### 新增待办一：`docs/` 里有两处已经和线上对不上

练功房那批改动**一个字都没动 `docs/`**（`git diff` 确认过）。结果是：

- `system-design-interview-playbook.md` 第六节还叫「把这些落到 kacam 的**问题池**」，
  正文在讨论要不要加一个「做技术」范畴，结尾写着「这是个需要确认的改动，**先别动代码**」。
  → 它已经落地了，而且**落的不是问题池是练功房**。这一节该重写成「为什么最后没走问题池」
  （`guidance` 原样进 system prompt、`MAX_USER_TURNS = 12` 撑不住六幕、
  陪聊的人格和判卷的人格兜不住），留着推导，改掉结论。
- `system-design-cases/README.md` 的「怎么用」还只讲「复制这个文件贴过去」，
  没提站上已经有 `/drills` 了。→ 补一句指过去。

### 新增待办二：note 记了但看不回来

README 里那句「下次重做同一题时先不看，做完再对比 —— **两次的 ❌ 有没有变少**，
比第一次答得好不好重要得多」是整套东西的复利所在。但站上现在承载不了：
`_attempts.html.erb` 只渲染**今天那一条** note，历次的记了就再也看不到。

最小且不越界的补法：**把历次 note 按日期列出来**。只列事实，
不算「❌ 少了几个」、不做趋势、不提醒该复习 —— 那些是判断，撞的是
「不给此刻加建议、打分、趋势诊断」那条边界。

（第一版提的「把复盘卡片贴回来存档并对比」——**对比那半句撤回**，
存档这半句 `note` 已经能承载，缺的只是显示。）

### 新增待办三：新系列上手前先定两张表

`tags` 是**按系列的词表，写错当场炸**（`Drill#tags_are_known`），`section` 是系列内的分组。
所以每个新系列开工前要先定：

- **标签词表**：A 型从它的 playbook 第二节长出来；B 型可能压根不需要标签
  （`sections: []` 和空词表在代码里是允许的，`section_is_known` 有那条 `blank?` 分支）
- **分组**：系统设计是 `featured` / `special`，B 型系列大概率只有一组

## 六、下一步

按第三节那个结论，建议顺序是：

1. **先写 `_authoring.md`**（收窄版，只管提示词那半边）—— 它是后面所有系列的模具，
   而且现在只有一个系列，正是抽模具最便宜的时候。
2. **第一个新系列做「复盘教练」**（B 型，2~3 份，零前置素材）——
   用它验证 B 型模板，也验证「加一个系列要动的五处」那张清单跑不跑得通。
3. **顺手清掉待办一那两处过时的 docs**（改动很小，但留着会持续误导）。
4. 补上待办二（历次 note 可见）。
5. 之后再决定要不要为「产品设计」写那份 playbook —— 那是这批里最大的一块投入，
   值得单独拍板。
