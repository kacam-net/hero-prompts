# 练功房：系列怎么扩

> 这份是**讨论稿**，改过两轮。第一版写在练功房落地之前；第二版按已实现的代码重审；
> 这一版（2026-09-05）是做完第二个系列之后的修正 ——
> **上一版最大的问题是把「粒度」漏掉了**，见第二节。
>
> **架构那部分不在这里** —— 「加一个系列要动哪五处」是 AGENTS.md 的活，别抄第二份。
> 这份只回答：**一份 Drill 该有多具体、哪些题成立、按什么顺序做。**

---

## 一、Drill 是什么（六个组件，只有一个不可省）

| 组件 | 干什么 |
| --- | --- |
| **角色反转** | 模型当考官/教练，不当答案库 |
| **铁律** | 压住大模型的默认人格（不许夸、一次一问、不剧透） |
| **分幕流程** | 每幕有「你要判定的」和「通过标准」 |
| **口令** | 用户随时能换挡 |
| **底牌** | **事先写死的、这道题独有的判卷清单** |
| **复盘卡片** | 可存档、可跨次对比的产出 |

**六件里只有底牌不可省**，因为它是这个板块**唯一的护城河**。

正面回答那个必须回答的问题 ——「用户直接去大模型那儿聊，和从这里复制一份提示词过去，
差别在哪？」只有四条，而且**头两条都依赖题目足够具体**：

1. **底牌。** 用户不知道自己该被问什么。一份写死的必答点清单是他给不了自己的东西。
2. **题目本身是策展。** 面对空对话框，人不知道该练哪一块。「练什么」是编辑决策，不是模型能给的。
3. 不许剧透、不许夸的纪律（模型的默认人格反过来）。
4. 可存档、能跨次对比的卡片格式。

第 3、4 条任何一份提示词都能带上，**所以真正的差别是 1 和 2**。它们一旦塌了，
这个板块就退化成「替用户省了几句打字」。

## 二、粒度：一份 Drill 是一道题，不是一套流程

**这是上一版漏掉的那条，也是第二个系列做偏的原因。**

### 判据

> **把这一份的底牌拿到同系列的另一份上去 —— 还成立吗？**
> **成立，说明粒度太粗：那不是底牌，是流程模板。**

拿现有的东西过一遍：

| 题 | 底牌 | 挪到隔壁 | 判定 |
| --- | --- | --- | --- |
| 秒杀 | 热点写、预扣与超时释放、少卖 vs 超卖、Redis 与 DB 一致性 | 挪到「支付」上完全不成立 | ✅ 粒度对 |
| 支付 | 单据模型、对账、TCC 三个经典问题 | 挪到「短链」上完全不成立 | ✅ |
| 复盘一个做完的项目 | 要数字、归因落到你能改的那一步、行动项可验证 | **挪到「复盘一次故障」上照样成立** | ❌ 太粗 |
| 方案答辩 | 追代价、必问三个备选、列前提假设 | **挪到任何一个方案上都成立** | ❌ 太粗 |

「复盘一个项目」不是一道题，是**一套可以套在任何东西上的流程**。它写得好不好是另一回事，
问题在于：这样一份东西，用户自己对模型说「你当教练复盘我这个项目，别夸我，追着要数字」
也能得到八成。**我们没有提供他给不了自己的东西。**

### 具象化 = 底牌回来了

关键在这儿：**题目一具体，底牌自己就长出来了。**

- 「复盘一个项目」→ 没有底牌可写
- 「复盘一次**上线延期**」→ 底牌立刻有了：延期的根因就那么几类（范围没锁、依赖没排、
  估算按顺利路径估的、返工来自需求理解偏差、有一个人是瓶颈），**每一类有它自己的征兆**，
  而征兆总是比实际爆发早两周。模型拿这份清单判「你说的那条属于哪一类、其余四类你查过没有」。

这就是为什么系统设计那批立得住而第二批立不住 —— 不是写作水平的差别，**是粒度的差别**。

### 顺带得到的两条

- **一个系列里的题应该「一眼能挑」。** 用户看列表时是在找「我眼下这件事对应哪一道」，
  而不是在找「哪套流程听起来更有道理」。题目越具体，越挑得动。
- **通用模板可以留，但只能当兜底。** 系统设计那批的 `00-generic-template.md` 就是先例，
  它自己写明了：「没有底牌，判卷会不如那 14 份准，**列在索引里的题优先用专门那一份**」。
  流程模板的正确位置是这个 —— 兜底，不是主体。

## 三、三种底牌 × 两种姿态

上一版按「底牌在哪」分了三型，那一维仍然对，但**漏了第二维：卡住的时候怎么办。**
面试类是对抗的，学习类必须引导 —— 同一套铁律套上去会毁掉学习类。

### 底牌从哪来（决定怎么判卷）

| 型 | 底牌 | 判卷靠 | 代表 |
| --- | --- | --- | --- |
| **A · 知识底牌** | 这道题的必答点 + 深水区 + 常见错答 | 覆盖表 ✅🟡❌ | 秒杀、支付、二分的边界、TLB |
| **B · 场景底牌** | 这**类场景**的常见根因/常见坑清单（用户带自己的材料来，但清单是我们写的） | 他说的那条属于哪一类，其余几类查过没有 | 复盘一次上线延期、答辩一次「重构 vs 重写」 |
| **C · 表现量表** | 维度评分表 | 当场表现 + 引原话当证据 | 英语口语、费曼讲解、谈判 |

**B 型的定义这一版改了。** 上一版写的是「底牌在你身上，所以没有底牌」——
那正是粒度出问题的根源。**正确的说法是：材料在用户身上，底牌在我们这儿。**
场景一具体，那类场景的坑就是可以事先写死的。写不出这份清单，说明场景还不够具体。

### 姿态（决定卡住时怎么办）

| 姿态 | 用在 | 卡住时 |
| --- | --- | --- |
| **拷问** | 面试、答辩、复盘 | 提示两次还不通才讲，讲完接着追代价。**答不上来是正常结局，不替他圆场** |
| **引导** | 学习类（算法、计算机系统、英语） | **不能照搬拷问那套** —— 学不会的人被追到底只会关掉窗口 |

**引导的四条纪律**（学习类的提示词里要原样写进去）：

1. **先问他卡在哪一层**，再给下一步：是没听过这个词 / 知道词但不知道为什么 /
   知道为什么但不会用？三种卡法要给的东西完全不同。
2. **给脚手架不给答案**：换个说法、举一个反例、拿他已经会的东西打比方、
   把问题拆成两个更小的。
3. **同一个点引导两次还不通才直接讲**，讲完**要求他用自己的话复述一遍** ——
   复述不出来就等于没学会，这一条比讲得对不对重要。
4. **仍然不许夸。** 这条跨所有姿态。引导不等于哄。

**学习类的底牌形状也不一样：底牌 = 常见的错误心智模型，不是知识点清单。**
教学的价值在纠错，不在灌输 —— 把知识点列一遍，模型自己就会；
「学这块的人通常会怎么想错」才是我们要提供的。

## 四、怎么把一个领域切成题

三种切法。**共同点是：切完之后每一份都要能通过第二节那个判据。**

### 切法一：按具体对象切（知识类）

不是「排序算法」，是**这一个技巧的不变量、边界和代价**。

### 切法二：按具体场景切（带自己的材料来）

不是「复盘」，是「复盘一次上线延期」。场景要具体到**它的根因是可以穷举的**。

### 切法三：按「掌握它需要什么」切（学习类）

**这一条最容易做错，因为教材目录是现成的诱惑。**

按目录切是：字母 / 自然拼读 / 短句 / 写作 —— 这是**内容的分类**，不是**掌握度的分解**，
按它切出来的题，底牌只能是「这一节讲什么」，等于把教材复述一遍。

正确的切法是：**先划出一小块，再问「学好这一块实际上要掌握哪几件事」，
那份清单就是底牌。**

## 五、三个示范（证明这个方法真的切得动）

### 算法

**它的具体性是白送的**，所以是验证新套路最便宜的一个系列。一份 = 一个技巧：

| 题 | 这一份独有的底牌 |
| --- | --- |
| 二分的边界 | 三种写法各自的不变量；死循环出在哪一步；为什么 `l < r` 和 `l <= r` 的收敛条件不同 |
| 单调栈 | 什么形状的问题会塌成它；栈里存的到底是什么；三个典型错法 |
| 滑动窗口 | 窗口什么时候不能收缩（这是它和双指针的分界） |
| 并查集 | 路径压缩和按秩合并各自省了什么、代价是什么 |
| 动态规划的状态定义 | 为什么状态定义是最难的一步（拿两道状态不同但答案相同的题对照） |
| 快排为什么实际比堆排快 | 缓存局部性；什么时候这条不成立 |

每份底牌 = **那个技巧的不变量 + 三个典型错法 + 复杂度的真实代价**。
互相挪一下完全不成立 → 粒度对。

### 计算机系统

**两级收窄：部分 → 块。一份 Drill 只学一块。**

| 部分 | 块（一份一块） |
| --- | --- |
| 内存 | 虚拟地址到物理地址走了哪几步、TLB miss 到底贵在哪 |
| 内存 | `malloc` 向内核要了什么、什么时候才真的分配物理页 |
| 并发 | 为什么需要内存屏障（重排序发生在哪一层，编译器还是 CPU） |
| 存储 | 一次 4KB 随机读的时间都花在哪 |
| 网络 | 建连到第一个字节到达之间发生了什么 |
| 进程 | 一次上下文切换贵在哪（不是保存寄存器那几十个周期） |

每份底牌 = **这一块的正确心智模型 + 三个常见的错误心智模型**。

### 英语

按你说的那个方法切。**「26 个字母」是一块，但这一块的内容不是「背 26 个字母」，
而是「学好它实际要掌握的这五件事」**：

1. **字母名 ≠ 字母音**（A 叫 /eɪ/，但在 cat 里读 /æ/）—— 这是后面自然拼读的全部地基，
   也是最多人跳过的一条
2. 五个元音字母和 21 个辅音字母：**为什么这个分类比字母顺序重要得多**
3. 形近易混：b/d、p/q、m/n、i/l —— 各自的辨认锚点
4. 音近易混：读出来分不清的那几组
5. 大小写不是装饰，是**句子边界和专名的信号**

底牌 = 这五条 + 每条一道自测题 + 每条上「学的人通常怎么想错」。
Drill 的活儿是逐条问，他卡住就按第三节那四条引导。

**这一份的判据也换了**：不是「答对几条」，是「**他能不能用自己的话把这五条讲一遍**」。

## 六、场景清单

「型」见第三节；「切成几份」按第四节的切法估。

### 已落地

| 系列 | 型 | 姿态 | 粒度 |
| --- | --- | --- | --- |
| **系统设计** | A | 拷问 | ✅ 一题一份独有底牌（17 份） |
| **讲自己的** | B | 拷问 | ✅ 具体场景 8 份 + 通用兜底 3 份 |
| **算法** | A | **引导** | ✅ 一份一个技巧（10 份） |
| **计算机系统** | A | **引导** | ✅ 一份一块（10 份） |

### 技术

| 场景 | 型 | 姿态 | 切成几份 | 备注 |
| --- | --- | --- | --- | --- |
| **线上故障排查** | A（反转） | 拷问 | 5~8 | 模型给症状，你提问诊断。底牌 = 真相 + 一串伪线索 |
| **数据库** | A | 引导 | 8~12 | 索引怎么被用上/用不上、事务隔离级别各挡住了什么、慢查询会诊 |
| **网络与浏览器** | A | 引导 | 8~12 | 更适合做成快问快答，不是案例题 |
| **代码评审** | A | 拷问 | 5~8 | 给一段埋了坑的代码让他找。素材要人写，模型自造的质量不稳 |
| **前端系统设计** | A | 拷问 | 8~12 | 缺的是那份 playbook |

### 产品与商业

| 场景 | 型 | 姿态 | 切成几份 | 备注 |
| --- | --- | --- | --- | --- |
| **产品设计** | A | 拷问 | 10~15 | 和系统设计同构，**要先有一份产品设计的 playbook**，最大一块投入 |
| **指标设计** | A | 拷问 | 3~5 | 一份一个指标，底牌 = 这个指标会被怎么作弊 |
| **商业点子拷问** | A/B | 拷问 | 2~3 | YC 式：谁付钱、为什么现在、为什么是你 |
| **定价** | A | 拷问 | 2~3 | 候补 |

### 求职与表达

| 场景 | 型 | 姿态 | 切成几份 | 备注 |
| --- | --- | --- | --- | --- |
| **行为面试 / 简历** | B | 拷问 | 2 | ✅ 已落地，粒度勉强够（行为面试内部已按七类切了） |
| **英语** | C + 学习 | 引导 | 按第五节那张分解表定 | 最复杂的一个：既要量表又要引导，放后面 |
| **费曼讲解** | C | 引导 | 3~5 | 素材成本几乎为零，通用性最强 |
| **薪资谈判** | C | 拷问 | 2~3 | 边界：不替人做决定、不预测结果 |
| **论文答辩 / 复试** | B | 拷问 | 2~3 | 和方案答辩同形 |

### 明确不收的两类

1. **人生决策和情绪心理**。没有底牌，硬做就是审问，而且必然滑向替人做决定 ——
   撞「做人那个范畴的边界和解梦同一条」那条红线。**这类归问题池，那边是陪聊不是拷问。**
2. **不需要提示词的东西**（读物、清单、速查表）。AGENTS.md 已经写死了。

## 七、「讲自己的」怎么补 ✅ 已补

诚实说：那五份**写得对但切得太粗**，五份的底牌互相可以换着用。

**不下架，降格成兜底**（照 `00-generic-template.md` 的先例，它自己就写着
「列在索引里的题优先用专门那一份」），然后在它们上面补一批具体场景。候选：

| 分组 | 补什么（一份一个场景，底牌 = 这类场景的根因清单） |
| --- | --- |
| 复盘 | 上线延期 ｜ 做了没人用的功能 ｜ 一次重构 ｜ 一次后来后悔的技术选型 |
| 复盘 | 数据不一致 ｜ 依赖的第三方挂了 ｜ 容量没扛住 ｜ 一次误操作 |
| 答辩 | 引入一个新中间件 ｜ 重构还是重写 ｜ 自研还是买 ｜ 要不要加这层缓存 |

每一份都要先过第二节那个判据：**底牌换到隔壁去还成立吗？** 不成立才写。

**已落地**（2026-09-05）：复盘补了上线延期 / 没人用的功能 / 数据不一致，
答辩补了重构还是重写 / 自研还是买 / 要不要加缓存，原来那三份移进 `generic` 组当兜底。
站上的文案也跟着改了 —— 原来写「这一批没有底牌」，现在不准了。

## 八、实现层面的几条约定（已写进 [drill-authoring.md](drill-authoring.md)）

1. **C 型的量表不要写成 `## 底牌`。** `Drill#answer_key?` 靠正文里有没有 `## 底牌`
   决定页面上提不提醒「别自己先读」。C 型的评分维度本来就该让人先看，
   写成底牌页面就会劝人别读一份该先读的东西 → 用 `## 评分维度`。
2. **但学习类的底牌仍然是底牌，照旧用 `## 底牌`。**（这一条上一版写反了：
   我原来说学习类的清单提前读到无所谓 —— 不对。「常见的想错」那一栏里
   写的就是每一问的答案，提前读了这一份就没得练了。变的只是**内容形状**
   （错误心智模型清单，不是必答点清单），不是要不要藏。
   真正该露出来的只有 C 型的评分量表：知道自己按流利度和结构被打分，
   不会泄露任何答案。）
3. **`locale` 是素材的语言，不是练习的语言。** 英语那批的提示词是中文写的指令
   （内容要求模型全程英文），属于 `zh-CN` 素材。塞进 `en` 的 `sources`，
   `display_locale` 会判成「中文界面没这个系列」，整个系列在中文站上消失。
4. **md 的标题起短。** 标题从正文第一行取、tagline 在清单里手写，
   两边都写成整句话的话，列表卡片上就是同一句说两遍。
5. **标签词表按系列各定各的**，不要求同构。

## 九、遗留待办

- **`docs/` 里两处和线上对不上**：`system-design-interview-playbook.md` 第六节还叫
  「把这些落到 kacam 的**问题池**」并写着「先别动代码」，而它落的是练功房；
  `system-design-cases/README.md` 的「怎么用」还只讲复制文件，没提站上已有 `/drills`。
- **历次 note 看不回来**：`_attempts.html.erb` 只渲染今天那一条。
  最小补法是按日期列出来 —— **只列不比**（算「❌ 少了几个」就越界成打分了）。
- ~~写那份规格~~ ✅ 落在 [drill-authoring.md](drill-authoring.md)
  （不叫 `_authoring.md` 了 —— 那个下划线是给 `docs/drills/` 那个没建成的目录
  准备的，而目录不搬那条已经撤回了，现在 `docs/` 是平的）。

## 十、算法这个系列做下来（2026-09-05）

十份，三组（基本功 / 数据结构 / 设计范式）。**它是第一个学习型系列**，
用来验证「切法一 + 引导姿态」。几条确认成立的和踩到的：

- **粒度判据真的能用来砍题。** 一开始的候选里有「排序」「哈希」「递归」，
  过判据的时候发现它们的底牌互相能换（都是「注意边界、注意复杂度」），
  砍掉换成了「二分的边界」「前缀和为什么能减」这种。**判据不只是事后检查，
  它在选题阶段就把题目逼具体了** —— 这是这次最大的收获。
- **引导姿态需要一条反剧透条款。** 学习类最大的风险不是模型太凶，是它太急着
  展示知识：一上来就说「这道题用单调栈」，用户就再也没机会自己认出这个形状了。
  所以十份里每一份都有那条「**在我说出可行做法之前，不许提到任何专有名词**」。
- **底牌形状变了但仍然要藏**（见第八节第 2 条那个更正）：这批的底牌主体是
  「常见的想错」，而那一栏写的就是每一问的答案。
- **口令是四个，而且有一个是新的**（`换个说法`）—— 又一次印证「说明一个系列
  一个 partial」：三个系列现在是四口令 / 三口令 / 四口令，其中两处的第二个口令不同。
- **踩到一个小的**：`tagline` 不过 markdown（只有 `intro` 过），
  写了反引号会原样显示在卡片上。

## 十一、下一步

1. ~~写那份规格~~ ✅ [drill-authoring.md](drill-authoring.md)
2. ~~补「讲自己的」的具体场景~~ ✅ 见第七节
3. ~~计算机系统系列~~ ✅ 10 份，四组。它顺带给判据补了一条：
   **说明 partial 要不要重写是必要条件，不是充分条件 —— 系列同时也是浏览单位。**
   计算机系统和算法的说明形状几乎一样，但想练 TLB 的人不会去「算法」下面找。
4. **清掉第九节那两处过时的 docs**。
5. **补上历次 note 可见**（第九节）。
6. 再往后：**线上故障排查**（A 反转，第一次验证「你问它答」那个变体）
   或者**英语**（C 型，只做一份把量表模板立起来）。
7. 「产品设计」那份 playbook 仍然是最大的一块投入，值得单独拍板。

至于**要不要上生成器把重复抽掉**：三个系列到齐了，结论是**暂时不上**。
22 份里真正逐字重复的只有那三条通用铁律，其余按型和姿态各有差别，
公共部分还不够养一个生成器；而规格文档已经把重复的成本从「手抄」降到「照模板填」。
