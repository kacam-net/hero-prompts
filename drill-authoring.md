# 怎么写一份 Drill

练功房里一份提示词的规格。三个系列 22 份写下来之后抽出来的模具。

**这份只管提示词正文那半边。** 代码侧要动哪五处（`DrillSeries::ALL` → md → 清单 →
说明 partial → 12 个 locale → import）在 AGENTS.md「加一个系列要动的地方」，
**别在这里抄第二份**，抄了就会两处不一致。
哪些系列值得做、按什么顺序做在 [drill-series-expansion.md](drill-series-expansion.md)。

---

## 第零步：过粒度这一关

> **把你要写的这一份的底牌，拿到同系列已有的另一份上去 —— 还成立吗？**
> **成立就说明粒度太粗：那不是底牌，是流程模板。回去把题目切细。**

| 题 | 底牌 | 挪到隔壁 | |
| --- | --- | --- | --- |
| 秒杀 | 热点写、预扣与超时释放、少卖 vs 超卖 | 挪到「支付」上完全不成立 | ✅ |
| 二分的边界 | 三种写法的不变量、死循环的不动点 | 挪到「单调栈」上完全不成立 | ✅ |
| 复盘一个做完的项目 | 要数字、追归因、行动项可验证 | **挪到「复盘一次故障」照样成立** | ❌ |

**这一条不是事后检查，是选题工具。** 算法那批选题时，「排序」「哈希」「递归」
都是这么被砍掉的 —— 一试才发现它们的底牌只能写成「注意边界、注意复杂度」，
互相能换。换成「二分的边界」「前缀和为什么能减」之后才立住。

**写不出这一份独有的底牌，说明题目还不够具体**，不是你不会写底牌。

三种切法：

| 切法 | 不是 | 而是 |
| --- | --- | --- |
| 按具体对象（知识类） | 「排序算法」 | 「快排为什么实际比堆排快」 |
| 按具体场景（带材料来） | 「复盘一个项目」 | 「复盘一次上线延期」 |
| 按「掌握它需要什么」（学习类） | 按教材目录切（字母 / 拼读 / 短句） | 「26 个字母」这一块 + **学好它实际要掌握的那五件事** |

第三种最容易做错，因为教材目录是现成的诱惑。**目录是内容的分类，不是掌握度的分解。**

## 第一步：选型

两个维度，先定下来，后面所有内容跟着它走。

### 底牌从哪来 → 决定怎么判卷

| 型 | 底牌是什么 | 判卷靠 | 例子 |
| --- | --- | --- | --- |
| **A · 知识底牌** | 必答点 + 深水区 + 常见错答 | 覆盖表 ✅🟡❌ | 秒杀、二分的边界 |
| **B · 场景底牌** | **这类场景**的根因/坑清单（材料是用户带的，**清单是我们写的**） | 他说的属于哪一类，其余几类查过没有 | 复盘一次上线延期 |
| **C · 表现量表** | 维度评分表 | 当场表现 + 引原话当证据 | 英语口语、费曼讲解 |

> **B 型不是「没有底牌」。** 材料在用户身上，底牌在我们这儿 ——
> 「讲自己的」那批就是把这条搞反才做粗的。

### 姿态 → 决定卡住时怎么办

| 姿态 | 用在 | 卡住时 |
| --- | --- | --- |
| **拷问** | 面试、答辩、复盘 | 提示两次才讲。**答不上来是正常结局，不替他圆场** |
| **引导** | 学习类 | **不能照搬拷问那套** —— 学不会的人被追到底只会关掉窗口 |

## 第二步：铁律

### 三条所有型都要有

1. **一次只问一个问题，问完就停。** 不要自问自答，不要一次抛三个，
   更不要在我回答之前先把可能的答案列出来。
2. **不要夸我。**「很好」「说得对」一句都别说。对就进下一问，不完整就直接指出缺什么。
   —— **这是唯一跨所有型、所有姿态都不变的那条。**
3. **允许我说「不知道」「记不清了」。** 记一笔，进下一问，
   **不要替我猜，也不要帮我把答案圆回来。**

### 按型加

**A 型加：**

- **不许剧透。** 你心里先把这道题的必答点、深水区、常见错答列一份清单
  （**不要显示给我**），之后拿它判卷。在我自己说出来之前，一个字都不要透。
- **每个结论都要追代价。**「用 X」就追「代价是什么」「X 挂了怎么办」
  「什么情况下这个方案是错的」。**只讲收益不讲代价的答案，一律当成背的。**
- **区分「知道」和「做过」。** 答对之后追具体的数字、故障、妥协。

**B 型加：**

- **不接受抽象词。**「效果不错」「高并发」「配合有问题」→ 立刻要具体：
  多少、哪一天、谁在什么时候说了什么。给不出的当场记一笔：**这句是空的**。
- **不接受「一般来说」「通常我们会」。** 一说就打断：**「你们当时是怎么做的？」**
- **不要替我把答案圆回来。** ——**无底牌感最强的那一类最容易死在这条上**：
  我说「记不清了」，模型接一句「可能是因为排期紧吧」，我一点头，
  那句就变成了我的记忆。这比答不出来糟糕得多。

**C 型加：**

- **不替我组织语言。** 我卡壳的时候不要给我句子，要我自己说完。
- **过程语言 = 目标语言。** 练英语就全程英语，我卡住才允许中文求助。
- **按维度给分，每一维都要引我的原话当证据。**

### 按姿态加

**拷问加：**

- 我卡住时给的是**提示** —— 换个角度问、举一个反例、指一个方向 —— 不是答案。
  **同一个点提示两次我还答不上来，才直接讲**，讲完接着追「那这么做的代价是什么」。
- **答不上来是这一幕的正常结局，不用替我圆场。**
- **不替我做决定。** 有多个方案时你把取舍摆清楚，选哪个我说了算。
  **我被质疑就立刻改口的话，要点出来** —— 那说明原方案不是我想出来的。

**引导加：**

- **卡住时先问我卡在哪一层**，再决定给什么：
  没听过这个概念 / 知道但不知道为什么 / 知道为什么但用不上。
  **三种要给的东西完全不同，别在问清之前就开始讲。**
- **给脚手架，不给答案。** 四种脚手架：换个说法、举一个反例让我自己撞上矛盾、
  拿我已经会的东西打比方、把问题拆成两个更小的。
- **同一个点引导两次还不通才直接讲，讲完要我用自己的话复述一遍。**
  复述不出来记一笔 —— 那等于没学会。
- **反剧透条款（学习类必须有）：在我自己说出一个可行做法之前，
  不许提到任何专有名词。**
  学习类最大的风险不是模型太凶，是它太急着展示知识 ——
  一上来就说「这道题用单调栈」，我就再也没机会自己认出这个形状了。

## 第三步：幕次

- **4~6 幕**，每幕 5~15 分钟。每幕开头说一句「第 N 幕 · XXX」，
  结束时用一两句话点评（缺什么、哪句是空的），再进下一幕。
- **主战场放第三或第四幕**，给它最多的时间，其余幕为它做铺垫。
- 每一幕要写清**「你要判定的」**和**「通过标准」**（A 型），
  或者**「这一幕必问的那一问」**（B / C / 学习类）。
- **每一份要有一句「中心问题」** —— 整份文件的存在理由，通常落在主战场那一幕。
  写完之后问自己：这份的中心问题是哪一句？说不出来就说明还没写完。

  > 「你维护的是哪个不变量？」（二分）
  > 「弹出栈顶的那一刻，你同时知道了哪几件事？」（单调栈）
  > 「滑动窗口成立的前提，用一句话说是什么？」（双指针）
  > 「这件事**你能改的部分**是哪一步？」（项目复盘）

- **第一幕的开场姿势按型定**：
  A 型「先把题面给我，然后闭嘴」；B 型「先用一两句话说出你要讲哪件事」；
  学习类「先给我你的暴力解，别急着最优」。

## 第四步：口令

**口令的数量和名字跟着型走**，这也是「说明一个系列一个 partial」的直接原因。

| 型 / 姿态 | 口令 |
| --- | --- |
| A 型 · 拷问 | `跳过` ｜ `提示` ｜ `直说吧` ｜ `复盘` |
| B 型 · 拷问 | `跳过` ｜ `换一个`（换的是什么就叫什么：`换个项目` / `换一条`）｜ `复盘`。**没有 `提示` 和 `直说吧`** —— 用户带的材料没有标准答案，硬留着这两个口令，模型只会现编一个答案出来 |
| 学习类 · 引导 | `跳过` ｜ `换个说法` ｜ `直说吧` ｜ `复盘` |
| C 型 · 角色扮演 | `跳过` ｜ `换个说法` ｜ **`暂停`**（跳出角色问一句元问题）｜ `复盘`。**`暂停` 是角色扮演型必须给的出口** —— 没有它，用户想问「刚才那句我该怎么说」都没地方问；而在角色里问，模型就得破坏设定 |

**口令在页面上写死，不走 `t()`**：它们是要原样打给模型的词，跟着中文正文走。
英文界面上把 `跳过` 译成 skip 打过去，模型是不认的（同曲名不翻译那条）。

## 第五步：收尾卡片

三型的格式不一样，但三条共通：

- **可存档**，而且**下一次做同一份时能和上一次并排比**。
- 每一条判定都要**引用户的原话**当证据。
- **最后一句固定是「一句话总评。不要客气，也不要把评价写成鼓励。」**

| 型 | 卡片主体 |
| --- | --- |
| **A** | ① 必答点覆盖表 ✅🟡❌ ② 代价对照（说出代价的 vs 只讲收益的）③ 哪些是背的 ④ 三个洞 |
| **B** | ① 事实表（时间线 / 三个数字 / 预期 vs 实际）② **空话清单**（每个抽象词后面写「背后给了什么具体的」）③ 归因或可信度判定 ④ 三条可验证的行动项 |
| **C** | ① 逐维打分 + 原话证据 ② 「你说过的原句 → 更好的说法」③ 下次重点练哪一维 |
| **学习类** | 在 A 的基础上多一栏 **④ 复述检验**（我被直接讲过的点，复述得怎么样） |

## 第六步：底牌

**放在文件末尾**，前面加一句 **「未经我自己说出，以下一个字都不许透露。」**

| 型 | 小标题 | 内容 | 能不能让人先看 |
| --- | --- | --- | --- |
| A | `## 底牌` | 必答点清单 + 深水区 + 常见错答 | ❌ |
| B（场景） | `## 底牌` | 这类场景的根因分类 + **每类的征兆** | ❌ |
| 学习类 | `## 底牌` | 正确心智模型 + **「常见的想错」（主体）** | ❌ |
| C | `## 评分维度` | 维度定义 + 各档的样子 | **✅ 该让人先看** |

两条：

1. **`Drill#answer_key?` 是靠正文里有没有 `## 底牌` 决定页面上提不提醒
   「别自己先读」的。** 所以 C 型的量表**不要**写成 `## 底牌` ——
   知道自己按流利度和结构被打分不会泄露任何答案，页面上却会劝人别读一份该先读的。

   **但 C 型里有一类例外：角色扮演。** 如果对面手里有一份用户看不见的处境
   （谈判那几份里的预算、能动的杠杆、什么时候松口），那就是真的底牌，
   要用 `## 底牌`。**于是同一个系列里可以两种末尾并存**（「讲清楚」就是），
   这时候**说明 partial 上必须写清楚哪一组是哪一种**，否则用户会以为漏了。
2. **学习类的底牌仍然要藏。** 它的内容形状变了（错误心智模型，不是知识点清单），
   但那一栏写的就是每一问的答案。变的是形状，不是要不要藏。

**每份底牌末尾写一条判卷线**，一句话说清什么样才算真的会：

> 「说得出『我维护的不变量是 X，所以循环条件必须是 Y』的，才算真的会。」
> 「能给出交换论证、或者能构造出带具体数字的反例的，才算真的会。」
> 「只讲收益不讲代价的答案，一定是背的。」

## 文件的形状

```markdown
# 标题

> 难度 ★★★☆☆ ｜ 建议 35 分钟 ｜ 主考：……
> 干什么：把「<用户现在的说法>」逼成「<你要他说出来的东西>」。
> 用法：把下面分隔线之后的全部内容复制给一个**新对话**的大模型，然后……
>
> （有没有底牌、这一份和隔壁那份的区别，写在这里）

---

（提示词全文：铁律 → 口令 → 幕次 → 收尾产出 → 底牌）
```

- **第一个单独成行的 `---` 之后的全部内容**就是复制给模型的那段，包括底牌。
- `---` 之前那个 `>` 块**不解析**，是给读仓库的人看的。页面文案在清单里另写。
- **标题起短。** 标题从 md 第一行取、tagline 在清单里手写 ——
  两边都写成整句话的话，列表卡片上就是同一句说两遍。
- **每份自包含，宁可重复。** 使用方式是复制一个文件贴过去，抽公共部分就得贴两个。
  可读性输给可用性。

## 上线前自查

- [ ] **粒度**：这一份的底牌挪到同系列隔壁那份上，还成立吗？（成立 = 不合格）
- [ ] **中心问题**：说得出这一份的中心问题是哪一句吗？
- [ ] 三条通用铁律都在？（一次一问 / 不许夸我 / 允许说不知道）
- [ ] 型和姿态各自那几条铁律都在？学习类有没有那条反剧透条款？
- [ ] 口令表在，而且数量和型对得上？
- [ ] 收尾卡片能存档、能和下一次并排比？最后一句是「不要客气」那句？
- [ ] 底牌在末尾、标了「不得透露」、末尾有一条判卷线？
- [ ] C 型的量表用的是 `## 评分维度` 而不是 `## 底牌`？
- [ ] md 标题够短？分隔线以上是给读仓库的人看的、以下是可复制全文？
- [ ] 清单（yml）里的 `intro` **没有剧透底牌**？`tagline` 里**没有反引号**
      （它不过 markdown，会原样显示）？`tags` 都在这个系列的词表里？

## 什么时候该新开一个系列

> **说明 partial 要不要重写一遍？要，才是新系列；否则它只是现有系列里的一个 `section`。**

「复盘 / 答辩 / 面试」共用同一套铁律、同一份说明，所以它们是一个系列里的三个分组，
不是三个系列。

一个系列的成本是**一个 partial + 12 个 locale 的一组文案 + 一条系列导航**；
一个分组的成本是**一行**。别为两道题开一个系列。

**但这条是必要条件，不是充分条件：系列同时也是浏览单位。**
「计算机系统」和「算法」的说明形状几乎一样（都是学习型、引导姿态、四个口令），
按上面那句它该是算法里的一个分组 —— 还是给了独立系列，
因为**想练 TLB 的人不会去「算法」下面找**。

所以完整的判据是两条，**都成立才开新系列**：

1. 说明要重写一遍（形状不同），**或者**
2. 它是用户会单独去找的一批东西（浏览单位不同）

两条都不成立 → 它是一个 `section`。
