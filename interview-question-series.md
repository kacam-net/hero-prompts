# 「面试快问」：按面试维度切的那个系列

> 设计稿（2026-09-13）。这一份回答三件事：
> **这个系列凭什么成立**、**一句八股怎么撑满 40 分钟**、**题库有哪些**。
>
> 规格仍然是 [drill-authoring.md](drill-authoring.md)，这份只写它和别的系列不一样的地方。

---

## 一、先过那一关：这不是「八股清单」吗

`computer-systems/README.md` 里写死过一句：

> **不做「八股清单」。** 十份都是「一次具体的操作在硬件上发生了什么」，
> 不是「进程和线程的区别」那种。后者只会得到一段名词解释。

**那句话没错，但它说的是「学习型系列里不做」。** 换到面试维度上，结论反过来 ——
因为这两个系列练的根本不是同一件事：

| | 计算机系统 / 数据库 / 算法 | **面试快问** |
| --- | --- | --- |
| 用户的处境 | 我想把这块学懂 | **我下周面试，这句话我会被问到** |
| 题目形状 | 一次具体操作在硬件上发生了什么 | **面试官真会开口的那一句原话** |
| 姿态 | 引导（学不会的人被追到底会关窗口） | **拷问**（面试本来就是对抗的） |
| 底牌主体 | 常见的想错（错误心智模型） | **背诵指纹**（被背烂的那几句原话 + 每句往哪追） |
| 交付物 | 你能用自己的话讲一遍 | **一段 60 秒、当场说得出口的回答** |

「进程和线程的区别」作为一道**学习题**确实只能得到名词解释；
作为一道**面试题**，名词解释恰恰是它的起点 —— 我们要的是从那段名词解释开始往下挖五层。

### 粒度判据过不过

拿 `drill-authoring.md` 第零步那把尺子量：

| 题 | 这一份独有的底牌 | 挪到隔壁 | |
| --- | --- | --- | --- |
| 进程和线程的区别 | Linux 下两者都是 task_struct、差的是 clone 时共享了哪几张表；创建/切换的数量级；多线程程序里 fork 会怎样 | 挪到「TCP 和 UDP 的区别」上完全不成立 | ✅ |
| 缓存穿透击穿雪崩 | 三个词被混用的那几种混法；布隆过滤器不能删这一条代价 | 挪到「分布式锁」上完全不成立 | ✅ |
| ~~常见的面试八股题~~ | 「答出定义、追问代价、要数字」 | **挪到任何一题都成立** | ❌ |

**过关的原因是「背诵指纹」这一栏。** 知识点模型自己会列，
但**「这一问被背烂的那几句原话分别是什么、听到哪一句该往哪个方向追」是写死的编辑决策** ——
这正是用户自己对着空对话框给不了自己的东西，也是这个系列的护城河。

### 它是新系列，不是分组

按 `drill-authoring.md` 最后一节那两条判据，两条都成立：

1. **说明要重写**：六幕的形状是新的（第一幕录基线、最后一幕回炉重讲），
   口令多一个 `重讲`，收尾卡片主体是「开场版 vs 收尾版对照」。
2. **它是独立的浏览单位**：下周要面试的人不会去「计算机系统」下面找「进程和线程」。

---

## 二、一句八股怎么撑满 40 分钟：五层阶梯

这是整个系列的机械核心。**每一道题的底牌都按这五层填，幕次也按这五层走。**

| 层 | 问的是 | 答不上来说明 |
| --- | --- | --- |
| **L1 · 定义** | 这几个词各自是什么、区别列几条 | 没背过 |
| **L2 · 机制** | 这个区别在系统里**具体是哪个结构、哪一步**造成的 | **背过，但只背了结论** |
| **L3 · 数字** | 差多少？几微秒、几倍、几次 IO | 没上过手 |
| **L4 · 选择** | 给一个具体场景，选哪个、代价是什么、什么时候这个选择是错的 | 不会用 |
| **L5 · 边界** | 你刚才那套说法在什么情况下**不成立** | 背得全，但没真懂 |

**通用判卷线：到 L4 才算能过面试，到 L5 才算真的懂，只有 L1 的一律记成背的。**

八股题聊不下去，从来不是因为题目浅，是因为**没人接着往 L2 以下问**。
这张阶梯就是用来接着问的。每份底牌把五层各自填满，模型手里就有 40 分钟的弹药。

---

## 三、底牌的新形状：背诵指纹

每份底牌六栏，**第三栏是别的系列没有的**：

1. **一段合格的 60 秒回答** —— 标准交付物，模型拿它和用户最后那版对照
2. **五层必答点** —— L1~L5 逐层写死
3. **背诵指纹** ⭐ —— 这一问被背烂的那几句**原话**，**每句后面写清「听到它往哪追」**
4. **深水区** —— 问到这儿还答得上的，是真的懂（通常只有 2~4 条）
5. **常见错答** —— 事实性错误，不是「答得浅」
6. **判卷线** —— 一句话说清什么样才算真的会

指纹栏长这样（摘自 `01-process-vs-thread.md`）：

| 背出来的那句 | 听到就追 |
| --- | --- |
| 「进程是资源分配的最小单位，线程是 CPU 调度的最小单位」 | 「资源」具体指哪几张表？哪几样**不**共享？ |
| 「线程比进程轻量」 | 轻多少？创建各要多久？轻在哪一个步骤上？ |

**这一栏是这个系列真正的产出。** 没有它，模型听到那句标准答案会点头进下一题 ——
那就退化成「用户自己对模型说『你来面我』」了。

---

## 四、幕次：六幕，头尾是新的

| 幕 | 干什么 | 时长 |
| --- | --- | --- |
| **一 · 先把你背的那版说完** | **不打断**，让他一口气讲完。模型对照指纹表记下命中了哪几条，**当场不点评** | 3 min |
| **二 · 拆词**（L1） | 他说的每个名词往下拆一层 | 8 min |
| **三 · 机制与数字**（L2+L3，**主战场**） | 这一份的中心问题落在这里 | 15 min |
| **四 · 逼选型**（L4） | 给具体场景要他选，追代价、追反例、追「你们当时怎么做的」 | 10 min |
| **五 · 边界**（L5） | 你那套说法在哪儿失效 | 8 min |
| **六 · 回炉重讲** | **再给一遍 60 秒版本**，和第一幕那版并排比 | 5 min |

**第一幕和第六幕是这个系列独有的。** 理由：面试现场要交付的不是「你懂」，
是**一段说得出口的话**。录基线 → 挖五层 → 重讲，这条链才对得上真实场景，
也让收尾卡片天然可跨次对比。

第一幕有一条必须写进铁律：**这一幕不许打断、不许点评、不许提示。**
模型的默认人格会忍不住在第三句就开始纠正 —— 那样基线就没了。

---

## 五、口令

A 型拷问那四个 + 一个新的：

`跳过` ｜ `提示` ｜ `直说吧` ｜ **`重讲`**（直接跳到第六幕，重录 60 秒版本并和开场版对比）｜ `复盘`

`重讲` 是这个系列必须给的出口：练到一半发现自己开场那版讲得稀烂，
想立刻重讲一遍再继续 —— 没有这个口令就只能等到最后。

---

## 六、收尾卡片

| | 栏目 |
| --- | --- |
| ① | **开场版 vs 收尾版对照** —— 两段原话并排，逐条标「补上了什么 / 还是没说」 |
| ② | **五层到达表** L1~L5 逐层 ✅🟡❌ |
| ③ | **指纹命中清单** —— 命中了哪几句，每句被追问之后接住了没有 |
| ④ | **数字表** —— 每个数量级标 对 / 差一个数量级 / 说不出 |
| ⑤ | 三个洞 |
| ⑥ | **一句话总评。不要客气，也不要把评价写成鼓励。** |

---

## 七、题库

七组。每题后面那一句是**这一份独有的底牌指向** —— 写不出这一句的题不收。

A~E 五组三十份是语言中立的；F、G 两组各自绑死一门语言，**用户按自己的语言挑一组**，
另一组和他无关（所以站上这两组要分开列，不要混排）。

### A · 操作系统与并发

| # | 题 | 独有底牌指向 |
| --- | --- | --- |
| A1 | **进程和线程的区别** | 内核里都是 task_struct，差的是 clone 的共享标志；多线程程序里 fork 会怎样 |
| A2 | **同步、异步、阻塞、非阻塞四个词** | 四象限里哪一格是空的；「异步」在不同层被用成三种意思 |
| A3 | **select、poll、epoll 的区别** | 每次调用拷贝了什么；就绪列表；水平触发和边缘触发漏事件的具体写法 |
| A4 | **死锁：怎么产生、怎么破、怎么查** | 四个条件不是重点，**破哪一条最便宜**才是；线上怎么拿到等待图 |
| A5 | **乐观锁和悲观锁** | CAS 的 ABA、版本号落到哪一列；高冲突下乐观锁的重试放大 |
| A6 | **线程池的参数怎么定、满了会怎样** | 四种拒绝策略；无界队列把 maxPoolSize 变成死参数 |

### B · 网络

| # | 题 | 独有底牌指向 |
| --- | --- | --- |
| B1 | **TCP 和 UDP 的区别** | 「可靠」不等于「一定送到」；拥塞控制才是真正的分界；QUIC 为什么在 UDP 上重做一遍 |
| B2 | **三次握手为什么不是两次、TIME_WAIT 为什么是 2MSL** | 两次不行的那个具体反例；TIME_WAIT 堆积线上怎么处理、`tw_reuse` 的代价 |
| B3 | **HTTPS 凭什么安全（TLS 握手）** | 非对称只用来换密钥；证书链验的到底是什么；中间人在哪一步被挡住 |
| B4 | **从输入 URL 到页面出来** | 每一步的耗时量级；面试官必打断的那三处 |
| B5 | **HTTP/1.1、2、3 的区别** | 队头阻塞有两层（HTTP 层和 TCP 层），2 只解决了一层 |
| B6 | **Cookie、Session、Token、JWT** | JWT 不能主动失效这一条代价；多机房下 Session 怎么办；放哪儿各自防谁 |
| B7 | **四层和七层负载均衡** | 能看到什么就能做什么；长连接下负载为什么会不均 |

### C · 数据库（与 [database/](database/) 分工见第九节）

| # | 题 | 独有底牌指向 |
| --- | --- | --- |
| C1 | **为什么索引用 B+ 树，不用哈希/B 树/跳表** | 三个对手各自输在哪一条；树高和磁盘 IO 次数的实际数字 |
| C2 | **四种隔离级别、幻读是什么** | RR 下幻读到底有没有这个经典陷阱；每一级具体挡住哪一类 |
| C3 | **MVCC 是怎么实现的** | ReadView 那几个字段；版本链；快照读和当前读的分界 |
| C4 | **redo log、undo log、binlog 和两阶段提交** | 崩溃恰好发生在每一个时间点上会怎样 |
| C5 | **分库分表之后失去了什么** | 跨库 join、分布式 ID、深翻页、扩容那次迁移 |

### D · 缓存与消息

| # | 题 | 独有底牌指向 |
| --- | --- | --- |
| D1 | **Redis 为什么快、单线程指的是哪一部分** | 「单线程」的边界；6.0 的 IO 线程做了什么；快的原因按贡献排序 |
| D2 | **穿透、击穿、雪崩** | 三个词被混用的那几种混法；布隆过滤器不能删元素这一条代价 |
| D3 | **缓存和数据库怎么保持一致** | 先删后写/先写后删各自的时序反例；延迟双删为什么也不保证 |
| D4 | **RDB、AOF 和主从复制** | fork 时的写时复制抖动；主从是异步的，所以切换一定会丢 |
| D5 | **分布式锁怎么做才对** | setnx 的四个坑：过期、误删、续期、主从丢锁 |
| D6 | **消息怎么做到不丢不重** | 三段各自的机制；幂等落到哪张表；exactly-once 是怎么被包装出来的 |

### E · 分布式与架构

| # | 题 | 独有底牌指向 |
| --- | --- | --- |
| E1 | **CAP 和 BASE** | P 不是可选项；这里的 C 是线性一致不是事务的 C；注册中心该选 CP 还是 AP |
| E2 | **分布式事务几种方案怎么选** | 2PC 阻塞在哪一步；TCC 的空回滚/悬挂/幂等三个坑 |
| E3 | **分布式 ID 怎么生成** | 雪花的时钟回拨怎么办；为什么不拿 UUID 当主键 |
| E4 | **限流：三种算法和真实部署** | 令牌桶和漏桶差在对突发的态度；单机限流在多实例下失效 |
| E5 | **幂等怎么设计** | 唯一键 > token > 状态机；「重复」要先定义谁重复、重复多久 |
| E6 | **熔断、降级、隔离** | 三件事不是一回事；半开状态怎么放量；线程池隔离 vs 信号量隔离 |

### F · Java 与 JVM（**绑定实现**，照数据库那批绑 MySQL 的先例）

| # | 题 | 独有底牌指向 |
| --- | --- | --- |
| F1 | **Java 是值传递还是引用传递** | 超高频、极易答错，而且能一路问到栈帧 |
| F2 | **JVM 内存区域，一个对象的一生** | 分配在哪、什么时候进老年代、栈上分配和逃逸分析 |
| F3 | **GC 怎么判断垃圾、一次 Full GC 意味着什么** | 可达性分析的 GC Roots 有哪几类；STW 的实际毫秒数 |
| F4 | **HashMap 的原理、它的线程不安全表现在哪** | 扩容那一刻具体丢了什么；为什么 8 转红黑树 |
| F5 | **synchronized 和 Lock、锁升级四个状态** | 每一级升级的触发条件；偏向锁后来为什么被废掉 |
| F6 | **OOM 和内存泄漏怎么定位** | 八种 OOM 各自指向哪；堆外泄漏为什么在堆 dump 里看不见 |

### G · Go 与运行时（**同样绑定实现**，和 F 组并行，不是它的翻译版）

Go 组不是把 Java 那六道换个语言问一遍 —— **两边被追问的方向不一样**：
Java 面试挖的是 JVM 这一层，Go 面试挖的是调度器和内存模型。所以选题各选各的。

| # | 题 | 独有底牌指向 |
| --- | --- | --- |
| G1 | **GMP 调度模型** | P 的数量为什么等于 GOMAXPROCS；work stealing；陷入系统调用时 M 和 P 怎么解绑 |
| G2 | **channel 的底层和 select** | 无缓冲 channel 是同步交接不是「长度 0 的队列」；关闭的三条规则；nil channel 在 select 里的正当用法 |
| G3 | **slice 和 map 的那些坑** | append 扩容之后共享底层数组悄悄失效；map 元素为什么不可寻址、遍历为什么故意随机 |
| G4 | **defer、panic、recover** | defer 的参数**立刻求值**；defer 改得动命名返回值；recover 救不了别的 goroutine |
| G5 | **逃逸分析：这个变量在栈上还是堆上** | 四类典型逃逸；`-gcflags=-m` 怎么看；Go 的栈可增长，所以「栈上分配」的边界和 C 不一样 |
| G6 | **Go 的 GC 和 STW 到底停多久** | 三色标记 + 混合写屏障；两次短 STW 各在哪；GOGC 调的是什么；为什么不做分代 |

**合计 42 份。**

### 候补，不进第一批

- **前端组**（事件循环、原型链、闭包、虚拟 DOM、浏览器渲染）—— 面向的人群不同，值得单独一组
- **云原生组**（容器和虚拟机的区别、K8s 的 Pod 为什么不是容器、service mesh）

---

## 八、明确不收

1. **要动手写的**：算法题、手写单例/生产者消费者、SQL 手写、白板画图。
   **判据一句话：闭着眼睛用嘴说得清楚吗？说不清楚的不收。** 这批练的是视频面试那半边。
2. **纯记忆无追问空间的**：「HTTP 状态码有哪些」「Linux 常用命令」——
   过不了粒度判据，底牌只能写成一张表。
3. **带自己材料的**：「讲一个你做过的项目」「你最大的缺点」——
   那是 [your-own/](your-own/) 和行为面试那一组的活，B 型，不是这里。

---

## 九、和已有系列的重叠，怎么划

有四处会撞，**处理方式统一：题目形状不同就都留，并在两边索引里互相指一句。**

| 撞的地方 | 已有 | 这里 | 怎么分 |
| --- | --- | --- | --- |
| 数据库 | `database/` 8 份，学习型引导，钻「这条索引为什么没走」 | C 组 5 份，拷问，问「为什么用 B+ 树」 | **一个练排查，一个练口述。** 索引里写：想学懂去那边，下周面试来这边 |
| 并发/内存 | `computer-systems/` 10 份，逼数量级 | A 组 | 那边是「一次上下文切换贵在哪」，这边是「进程和线程的区别」——**同一块知识的两个入口** |
| 缓存一致性、限流 | `system-design-cases/` 里作为一个环节出现 | D3、E4 单独成题 | 那边是在一个案例里顺带追一句，这边是把这一句问满 40 分钟 |
| OOM 定位 | `troubleshooting/03-memory-creep.md`，反转型（你问它答） | F6，正着问 | 姿态完全相反，不冲突 |

**不为了避嫌砍题。** 判据是粒度，不是「有没有出现过这个名词」。

---

## 十、落地状态

> **✅ 全部 42 份已落地（2026-09-13）**，见 [interview-questions/](interview-questions/)。
> 系列 README、根 README 的目录表、
> [drill-series-expansion.md](drill-series-expansion.md) 第六节与第十三节都已同步。

样板是 [01-process-vs-thread.md](interview-questions/01-process-vs-thread.md)：
五层阶梯、指纹表、六幕、`重讲` 口令、开场版 vs 收尾版卡片 ——
**往后加题照它填。**

**还没做的**：站上的策展清单（yml 的 `intro` / `tagline` / `tags` / 难度时长）
是主仓库那边的活儿，**合并进来不等于站上立刻能看到**。

跑一轮之后要复核的三件事：

1. **第一幕真的能让模型闭嘴吗？** 这是最可能失败的一条 —— 它天生想纠正。
   （`code-review/` 那批已经证明「第一幕模型闭嘴」是可行的，形状可以参考。）
2. **指纹表会不会反过来成了提示？** 模型若照着指纹表逐条念「你是不是想说 XX」，
   就等于剧透。铁律里要写死：**指纹表只用来决定往哪追，不许把那句话说出来。**
3. **40 分钟够不够 / 会不会太长。** 第一批全按 45 分钟标，跑完按实际改。
